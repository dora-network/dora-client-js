/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AffiliateCashFlow model module.
 * @module model/AffiliateCashFlow
 * @version 1.0.0
 */
class AffiliateCashFlow {
    /**
     * Constructs a new <code>AffiliateCashFlow</code>.
     * @alias module:model/AffiliateCashFlow
     * @param id {String} 
     * @param kind {module:model/AffiliateCashFlow.KindEnum} 
     * @param createdAt {Date} 
     * @param assetId {String} 
     * @param assetSymbol {String} 
     * @param amount {String} Decimal value, without binary floating-point rounding.
     */
    constructor(id, kind, createdAt, assetId, assetSymbol, amount) { 
        
        AffiliateCashFlow.initialize(this, id, kind, createdAt, assetId, assetSymbol, amount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, kind, createdAt, assetId, assetSymbol, amount) { 
        obj['id'] = id;
        obj['kind'] = kind;
        obj['created_at'] = createdAt;
        obj['asset_id'] = assetId;
        obj['asset_symbol'] = assetSymbol;
        obj['amount'] = amount;
    }

    /**
     * Constructs a <code>AffiliateCashFlow</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AffiliateCashFlow} obj Optional instance to populate.
     * @return {module:model/AffiliateCashFlow} The populated <code>AffiliateCashFlow</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AffiliateCashFlow();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('asset_id')) {
                obj['asset_id'] = ApiClient.convertToType(data['asset_id'], 'String');
            }
            if (data.hasOwnProperty('asset_symbol')) {
                obj['asset_symbol'] = ApiClient.convertToType(data['asset_symbol'], 'String');
            }
            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AffiliateCashFlow</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AffiliateCashFlow</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AffiliateCashFlow.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['kind'] && !(typeof data['kind'] === 'string' || data['kind'] instanceof String)) {
            throw new Error("Expected the field `kind` to be a primitive type in the JSON string but got " + data['kind']);
        }
        // ensure the json data is a string
        if (data['asset_id'] && !(typeof data['asset_id'] === 'string' || data['asset_id'] instanceof String)) {
            throw new Error("Expected the field `asset_id` to be a primitive type in the JSON string but got " + data['asset_id']);
        }
        // ensure the json data is a string
        if (data['asset_symbol'] && !(typeof data['asset_symbol'] === 'string' || data['asset_symbol'] instanceof String)) {
            throw new Error("Expected the field `asset_symbol` to be a primitive type in the JSON string but got " + data['asset_symbol']);
        }
        // ensure the json data is a string
        if (data['amount'] && !(typeof data['amount'] === 'string' || data['amount'] instanceof String)) {
            throw new Error("Expected the field `amount` to be a primitive type in the JSON string but got " + data['amount']);
        }

        return true;
    }


}

AffiliateCashFlow.RequiredProperties = ["id", "kind", "created_at", "asset_id", "asset_symbol", "amount"];

/**
 * @member {String} id
 */
AffiliateCashFlow.prototype['id'] = undefined;

/**
 * @member {module:model/AffiliateCashFlow.KindEnum} kind
 */
AffiliateCashFlow.prototype['kind'] = undefined;

/**
 * @member {Date} created_at
 */
AffiliateCashFlow.prototype['created_at'] = undefined;

/**
 * @member {String} asset_id
 */
AffiliateCashFlow.prototype['asset_id'] = undefined;

/**
 * @member {String} asset_symbol
 */
AffiliateCashFlow.prototype['asset_symbol'] = undefined;

/**
 * Decimal value, without binary floating-point rounding.
 * @member {String} amount
 */
AffiliateCashFlow.prototype['amount'] = undefined;





/**
 * Allowed values for the <code>kind</code> property.
 * @enum {String}
 * @readonly
 */
AffiliateCashFlow['KindEnum'] = {

    /**
     * value: "EXTERNAL_DEPOSIT"
     * @const
     */
    "EXTERNAL_DEPOSIT": "EXTERNAL_DEPOSIT",

    /**
     * value: "EXTERNAL_WITHDRAW"
     * @const
     */
    "EXTERNAL_WITHDRAW": "EXTERNAL_WITHDRAW"
};



export default AffiliateCashFlow;

