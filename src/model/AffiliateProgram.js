/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AffiliateProgram model module.
 * @module model/AffiliateProgram
 * @version 1.0.0
 */
class AffiliateProgram {
    /**
     * Constructs a new <code>AffiliateProgram</code>.
     * @alias module:model/AffiliateProgram
     * @param id {String} 
     * @param tenantId {String} Owning tenant for administration. This does not define a referral destination.
     * @param name {String} No surrounding whitespace or control characters.
     * @param description {String} Must not contain NUL characters.
     * @param isActive {Boolean} Inactive programs retain registrations, but cannot issue or resolve codes.
     * @param createdAt {Date} 
     * @param updatedAt {Date} 
     */
    constructor(id, tenantId, name, description, isActive, createdAt, updatedAt) { 
        
        AffiliateProgram.initialize(this, id, tenantId, name, description, isActive, createdAt, updatedAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, tenantId, name, description, isActive, createdAt, updatedAt) { 
        obj['id'] = id;
        obj['tenant_id'] = tenantId;
        obj['name'] = name;
        obj['description'] = description;
        obj['is_active'] = isActive;
        obj['created_at'] = createdAt;
        obj['updated_at'] = updatedAt;
    }

    /**
     * Constructs a <code>AffiliateProgram</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AffiliateProgram} obj Optional instance to populate.
     * @return {module:model/AffiliateProgram} The populated <code>AffiliateProgram</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AffiliateProgram();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('tenant_id')) {
                obj['tenant_id'] = ApiClient.convertToType(data['tenant_id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('is_active')) {
                obj['is_active'] = ApiClient.convertToType(data['is_active'], 'Boolean');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AffiliateProgram</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AffiliateProgram</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AffiliateProgram.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['tenant_id'] && !(typeof data['tenant_id'] === 'string' || data['tenant_id'] instanceof String)) {
            throw new Error("Expected the field `tenant_id` to be a primitive type in the JSON string but got " + data['tenant_id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }

        return true;
    }


}

AffiliateProgram.RequiredProperties = ["id", "tenant_id", "name", "description", "is_active", "created_at", "updated_at"];

/**
 * @member {String} id
 */
AffiliateProgram.prototype['id'] = undefined;

/**
 * Owning tenant for administration. This does not define a referral destination.
 * @member {String} tenant_id
 */
AffiliateProgram.prototype['tenant_id'] = undefined;

/**
 * No surrounding whitespace or control characters.
 * @member {String} name
 */
AffiliateProgram.prototype['name'] = undefined;

/**
 * Must not contain NUL characters.
 * @member {String} description
 */
AffiliateProgram.prototype['description'] = undefined;

/**
 * Inactive programs retain registrations, but cannot issue or resolve codes.
 * @member {Boolean} is_active
 */
AffiliateProgram.prototype['is_active'] = undefined;

/**
 * @member {Date} created_at
 */
AffiliateProgram.prototype['created_at'] = undefined;

/**
 * @member {Date} updated_at
 */
AffiliateProgram.prototype['updated_at'] = undefined;






export default AffiliateProgram;

