/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AffiliateReferrer model module.
 * @module model/AffiliateReferrer
 * @version 1.0.0
 */
class AffiliateReferrer {
    /**
     * Constructs a new <code>AffiliateReferrer</code>.
     * @alias module:model/AffiliateReferrer
     * @param id {String} 
     * @param programId {String} 
     * @param tenantId {String} Owning tenant for administration. This does not define a referral destination.
     * @param userId {String} 
     * @param referralCode {String} Reusable referral code, stored uppercase. Letters, digits, hyphens and underscores are accepted; the first character must be a letter or digit. Matching is case-insensitive. Separate from QR claim tokens.
     * @param createdAt {Date} 
     */
    constructor(id, programId, tenantId, userId, referralCode, createdAt) { 
        
        AffiliateReferrer.initialize(this, id, programId, tenantId, userId, referralCode, createdAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, programId, tenantId, userId, referralCode, createdAt) { 
        obj['id'] = id;
        obj['program_id'] = programId;
        obj['tenant_id'] = tenantId;
        obj['user_id'] = userId;
        obj['referral_code'] = referralCode;
        obj['created_at'] = createdAt;
    }

    /**
     * Constructs a <code>AffiliateReferrer</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AffiliateReferrer} obj Optional instance to populate.
     * @return {module:model/AffiliateReferrer} The populated <code>AffiliateReferrer</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AffiliateReferrer();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('program_id')) {
                obj['program_id'] = ApiClient.convertToType(data['program_id'], 'String');
            }
            if (data.hasOwnProperty('tenant_id')) {
                obj['tenant_id'] = ApiClient.convertToType(data['tenant_id'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('referral_code')) {
                obj['referral_code'] = ApiClient.convertToType(data['referral_code'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AffiliateReferrer</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AffiliateReferrer</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AffiliateReferrer.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['program_id'] && !(typeof data['program_id'] === 'string' || data['program_id'] instanceof String)) {
            throw new Error("Expected the field `program_id` to be a primitive type in the JSON string but got " + data['program_id']);
        }
        // ensure the json data is a string
        if (data['tenant_id'] && !(typeof data['tenant_id'] === 'string' || data['tenant_id'] instanceof String)) {
            throw new Error("Expected the field `tenant_id` to be a primitive type in the JSON string but got " + data['tenant_id']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['referral_code'] && !(typeof data['referral_code'] === 'string' || data['referral_code'] instanceof String)) {
            throw new Error("Expected the field `referral_code` to be a primitive type in the JSON string but got " + data['referral_code']);
        }

        return true;
    }


}

AffiliateReferrer.RequiredProperties = ["id", "program_id", "tenant_id", "user_id", "referral_code", "created_at"];

/**
 * @member {String} id
 */
AffiliateReferrer.prototype['id'] = undefined;

/**
 * @member {String} program_id
 */
AffiliateReferrer.prototype['program_id'] = undefined;

/**
 * Owning tenant for administration. This does not define a referral destination.
 * @member {String} tenant_id
 */
AffiliateReferrer.prototype['tenant_id'] = undefined;

/**
 * @member {String} user_id
 */
AffiliateReferrer.prototype['user_id'] = undefined;

/**
 * Reusable referral code, stored uppercase. Letters, digits, hyphens and underscores are accepted; the first character must be a letter or digit. Matching is case-insensitive. Separate from QR claim tokens.
 * @member {String} referral_code
 */
AffiliateReferrer.prototype['referral_code'] = undefined;

/**
 * @member {Date} created_at
 */
AffiliateReferrer.prototype['created_at'] = undefined;






export default AffiliateReferrer;

