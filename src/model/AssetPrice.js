/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AssetPrice model module.
 * @module model/AssetPrice
 * @version 1.0.0
 */
class AssetPrice {
    /**
     * Constructs a new <code>AssetPrice</code>.
     * @alias module:model/AssetPrice
     * @param assetId {String} 
     * @param price {String} 
     * @param time {Date} 
     */
    constructor(assetId, price, time) { 
        
        AssetPrice.initialize(this, assetId, price, time);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, assetId, price, time) { 
        obj['asset_id'] = assetId;
        obj['price'] = price;
        obj['time'] = time;
    }

    /**
     * Constructs a <code>AssetPrice</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AssetPrice} obj Optional instance to populate.
     * @return {module:model/AssetPrice} The populated <code>AssetPrice</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AssetPrice();

            if (data.hasOwnProperty('asset_id')) {
                obj['asset_id'] = ApiClient.convertToType(data['asset_id'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'String');
            }
            if (data.hasOwnProperty('ytm')) {
                obj['ytm'] = ApiClient.convertToType(data['ytm'], 'String');
            }
            if (data.hasOwnProperty('time')) {
                obj['time'] = ApiClient.convertToType(data['time'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AssetPrice</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AssetPrice</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AssetPrice.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['asset_id'] && !(typeof data['asset_id'] === 'string' || data['asset_id'] instanceof String)) {
            throw new Error("Expected the field `asset_id` to be a primitive type in the JSON string but got " + data['asset_id']);
        }
        // ensure the json data is a string
        if (data['price'] && !(typeof data['price'] === 'string' || data['price'] instanceof String)) {
            throw new Error("Expected the field `price` to be a primitive type in the JSON string but got " + data['price']);
        }
        // ensure the json data is a string
        if (data['ytm'] && !(typeof data['ytm'] === 'string' || data['ytm'] instanceof String)) {
            throw new Error("Expected the field `ytm` to be a primitive type in the JSON string but got " + data['ytm']);
        }

        return true;
    }


}

AssetPrice.RequiredProperties = ["asset_id", "price", "time"];

/**
 * @member {String} asset_id
 */
AssetPrice.prototype['asset_id'] = undefined;

/**
 * @member {String} price
 */
AssetPrice.prototype['price'] = undefined;

/**
 * @member {String} ytm
 */
AssetPrice.prototype['ytm'] = undefined;

/**
 * @member {Date} time
 */
AssetPrice.prototype['time'] = undefined;






export default AssetPrice;

