/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AssetYTM model module.
 * @module model/AssetYTM
 * @version 1.0.0
 */
class AssetYTM {
    /**
     * Constructs a new <code>AssetYTM</code>.
     * @alias module:model/AssetYTM
     * @param assetId {String} 
     * @param currentTime {Date} 
     * @param currentPrice {String} 
     * @param yieldToMaturity {String} 
     */
    constructor(assetId, currentTime, currentPrice, yieldToMaturity) { 
        
        AssetYTM.initialize(this, assetId, currentTime, currentPrice, yieldToMaturity);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, assetId, currentTime, currentPrice, yieldToMaturity) { 
        obj['asset_id'] = assetId;
        obj['current_time'] = currentTime;
        obj['current_price'] = currentPrice;
        obj['yield_to_maturity'] = yieldToMaturity;
    }

    /**
     * Constructs a <code>AssetYTM</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AssetYTM} obj Optional instance to populate.
     * @return {module:model/AssetYTM} The populated <code>AssetYTM</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AssetYTM();

            if (data.hasOwnProperty('asset_id')) {
                obj['asset_id'] = ApiClient.convertToType(data['asset_id'], 'String');
            }
            if (data.hasOwnProperty('current_time')) {
                obj['current_time'] = ApiClient.convertToType(data['current_time'], 'Date');
            }
            if (data.hasOwnProperty('current_price')) {
                obj['current_price'] = ApiClient.convertToType(data['current_price'], 'String');
            }
            if (data.hasOwnProperty('yield_to_maturity')) {
                obj['yield_to_maturity'] = ApiClient.convertToType(data['yield_to_maturity'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AssetYTM</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AssetYTM</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AssetYTM.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['asset_id'] && !(typeof data['asset_id'] === 'string' || data['asset_id'] instanceof String)) {
            throw new Error("Expected the field `asset_id` to be a primitive type in the JSON string but got " + data['asset_id']);
        }
        // ensure the json data is a string
        if (data['current_price'] && !(typeof data['current_price'] === 'string' || data['current_price'] instanceof String)) {
            throw new Error("Expected the field `current_price` to be a primitive type in the JSON string but got " + data['current_price']);
        }
        // ensure the json data is a string
        if (data['yield_to_maturity'] && !(typeof data['yield_to_maturity'] === 'string' || data['yield_to_maturity'] instanceof String)) {
            throw new Error("Expected the field `yield_to_maturity` to be a primitive type in the JSON string but got " + data['yield_to_maturity']);
        }

        return true;
    }


}

AssetYTM.RequiredProperties = ["asset_id", "current_time", "current_price", "yield_to_maturity"];

/**
 * @member {String} asset_id
 */
AssetYTM.prototype['asset_id'] = undefined;

/**
 * @member {Date} current_time
 */
AssetYTM.prototype['current_time'] = undefined;

/**
 * @member {String} current_price
 */
AssetYTM.prototype['current_price'] = undefined;

/**
 * @member {String} yield_to_maturity
 */
AssetYTM.prototype['yield_to_maturity'] = undefined;






export default AssetYTM;

