/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The BalanceTransfer model module.
 * @module model/BalanceTransfer
 * @version 1.0.0
 */
class BalanceTransfer {
    /**
     * Constructs a new <code>BalanceTransfer</code>.
     * @alias module:model/BalanceTransfer
     * @param fromPositionId {String} 
     * @param toPositionId {String} 
     * @param transactionId {String} 
     */
    constructor(fromPositionId, toPositionId, transactionId) { 
        
        BalanceTransfer.initialize(this, fromPositionId, toPositionId, transactionId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fromPositionId, toPositionId, transactionId) { 
        obj['from_position_id'] = fromPositionId;
        obj['to_position_id'] = toPositionId;
        obj['transaction_id'] = transactionId;
    }

    /**
     * Constructs a <code>BalanceTransfer</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BalanceTransfer} obj Optional instance to populate.
     * @return {module:model/BalanceTransfer} The populated <code>BalanceTransfer</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BalanceTransfer();

            if (data.hasOwnProperty('from_position_id')) {
                obj['from_position_id'] = ApiClient.convertToType(data['from_position_id'], 'String');
            }
            if (data.hasOwnProperty('to_position_id')) {
                obj['to_position_id'] = ApiClient.convertToType(data['to_position_id'], 'String');
            }
            if (data.hasOwnProperty('transaction_id')) {
                obj['transaction_id'] = ApiClient.convertToType(data['transaction_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BalanceTransfer</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BalanceTransfer</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BalanceTransfer.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['from_position_id'] && !(typeof data['from_position_id'] === 'string' || data['from_position_id'] instanceof String)) {
            throw new Error("Expected the field `from_position_id` to be a primitive type in the JSON string but got " + data['from_position_id']);
        }
        // ensure the json data is a string
        if (data['to_position_id'] && !(typeof data['to_position_id'] === 'string' || data['to_position_id'] instanceof String)) {
            throw new Error("Expected the field `to_position_id` to be a primitive type in the JSON string but got " + data['to_position_id']);
        }
        // ensure the json data is a string
        if (data['transaction_id'] && !(typeof data['transaction_id'] === 'string' || data['transaction_id'] instanceof String)) {
            throw new Error("Expected the field `transaction_id` to be a primitive type in the JSON string but got " + data['transaction_id']);
        }

        return true;
    }


}

BalanceTransfer.RequiredProperties = ["from_position_id", "to_position_id", "transaction_id"];

/**
 * @member {String} from_position_id
 */
BalanceTransfer.prototype['from_position_id'] = undefined;

/**
 * @member {String} to_position_id
 */
BalanceTransfer.prototype['to_position_id'] = undefined;

/**
 * @member {String} transaction_id
 */
BalanceTransfer.prototype['transaction_id'] = undefined;






export default BalanceTransfer;

