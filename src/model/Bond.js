/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BondKind from './BondKind';

/**
 * The Bond model module.
 * @module model/Bond
 * @version 1.0.0
 */
class Bond {
    /**
     * Constructs a new <code>Bond</code>.
     * @alias module:model/Bond
     * @param id {String} 
     * @param kind {module:model/BondKind} 
     * @param createdAt {Date} 
     * @param isin {String} 
     * @param issuedAt {Date} 
     * @param issuer {String} 
     * @param maturityAt {Date} 
     * @param principalValue {String} 
     * @param paymentsPerYear {Number} 
     */
    constructor(id, kind, createdAt, isin, issuedAt, issuer, maturityAt, principalValue, paymentsPerYear) { 
        
        Bond.initialize(this, id, kind, createdAt, isin, issuedAt, issuer, maturityAt, principalValue, paymentsPerYear);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, kind, createdAt, isin, issuedAt, issuer, maturityAt, principalValue, paymentsPerYear) { 
        obj['id'] = id;
        obj['kind'] = kind;
        obj['created_at'] = createdAt;
        obj['isin'] = isin;
        obj['issued_at'] = issuedAt;
        obj['issuer'] = issuer;
        obj['maturity_at'] = maturityAt;
        obj['principal_value'] = principalValue;
        obj['payments_per_year'] = paymentsPerYear;
    }

    /**
     * Constructs a <code>Bond</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Bond} obj Optional instance to populate.
     * @return {module:model/Bond} The populated <code>Bond</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Bond();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = BondKind.constructFromObject(data['kind']);
            }
            if (data.hasOwnProperty('coupon_start_at')) {
                obj['coupon_start_at'] = ApiClient.convertToType(data['coupon_start_at'], 'Date');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('final_coupon_at')) {
                obj['final_coupon_at'] = ApiClient.convertToType(data['final_coupon_at'], 'Date');
            }
            if (data.hasOwnProperty('isin')) {
                obj['isin'] = ApiClient.convertToType(data['isin'], 'String');
            }
            if (data.hasOwnProperty('issued_at')) {
                obj['issued_at'] = ApiClient.convertToType(data['issued_at'], 'Date');
            }
            if (data.hasOwnProperty('issuer')) {
                obj['issuer'] = ApiClient.convertToType(data['issuer'], 'String');
            }
            if (data.hasOwnProperty('maturity_at')) {
                obj['maturity_at'] = ApiClient.convertToType(data['maturity_at'], 'Date');
            }
            if (data.hasOwnProperty('principal_value')) {
                obj['principal_value'] = ApiClient.convertToType(data['principal_value'], 'String');
            }
            if (data.hasOwnProperty('payments_per_year')) {
                obj['payments_per_year'] = ApiClient.convertToType(data['payments_per_year'], 'Number');
            }
            if (data.hasOwnProperty('payments_every')) {
                obj['payments_every'] = ApiClient.convertToType(data['payments_every'], 'Number');
            }
            if (data.hasOwnProperty('next_coupon_payment')) {
                obj['next_coupon_payment'] = ApiClient.convertToType(data['next_coupon_payment'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Bond</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Bond</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Bond.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['isin'] && !(typeof data['isin'] === 'string' || data['isin'] instanceof String)) {
            throw new Error("Expected the field `isin` to be a primitive type in the JSON string but got " + data['isin']);
        }
        // ensure the json data is a string
        if (data['issuer'] && !(typeof data['issuer'] === 'string' || data['issuer'] instanceof String)) {
            throw new Error("Expected the field `issuer` to be a primitive type in the JSON string but got " + data['issuer']);
        }
        // ensure the json data is a string
        if (data['principal_value'] && !(typeof data['principal_value'] === 'string' || data['principal_value'] instanceof String)) {
            throw new Error("Expected the field `principal_value` to be a primitive type in the JSON string but got " + data['principal_value']);
        }

        return true;
    }


}

Bond.RequiredProperties = ["id", "kind", "created_at", "isin", "issued_at", "issuer", "maturity_at", "principal_value", "payments_per_year"];

/**
 * @member {String} id
 */
Bond.prototype['id'] = undefined;

/**
 * @member {module:model/BondKind} kind
 */
Bond.prototype['kind'] = undefined;

/**
 * @member {Date} coupon_start_at
 */
Bond.prototype['coupon_start_at'] = undefined;

/**
 * @member {Date} created_at
 */
Bond.prototype['created_at'] = undefined;

/**
 * @member {Date} final_coupon_at
 */
Bond.prototype['final_coupon_at'] = undefined;

/**
 * @member {String} isin
 */
Bond.prototype['isin'] = undefined;

/**
 * @member {Date} issued_at
 */
Bond.prototype['issued_at'] = undefined;

/**
 * @member {String} issuer
 */
Bond.prototype['issuer'] = undefined;

/**
 * @member {Date} maturity_at
 */
Bond.prototype['maturity_at'] = undefined;

/**
 * @member {String} principal_value
 */
Bond.prototype['principal_value'] = undefined;

/**
 * @member {Number} payments_per_year
 */
Bond.prototype['payments_per_year'] = undefined;

/**
 * Coupon payment frequency in nanoseconds (minimum 1000 i.e. 1 microsecond)
 * @member {Number} payments_every
 */
Bond.prototype['payments_every'] = undefined;

/**
 * @member {Date} next_coupon_payment
 */
Bond.prototype['next_coupon_payment'] = undefined;






export default Bond;

