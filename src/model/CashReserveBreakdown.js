/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CashReserveBreakdown model module.
 * @module model/CashReserveBreakdown
 * @version 1.0.0
 */
class CashReserveBreakdown {
    /**
     * Constructs a new <code>CashReserveBreakdown</code>.
     * Breakdown of the minimum USD cash reserve requirement.
     * @alias module:model/CashReserveBreakdown
     * @param completedPac {String} Completed PAC (partially accrued coupon) obligations the user owes, in USD.
     * @param outstandingLai {String} Outstanding LAI (leverage accrued interest) the user owes, in USD.
     * @param estimatedFees {String} Estimated trading fees for the current settlement period, capped at a configured fraction (1% by default) of the user's traded USD volume since 00:00:00 UTC.
     * @param borrowedPortion {String} Configured fraction (10% by default) of the user's total outstanding borrowed value, in USD.
     * @param floor {String} Configured absolute minimum requirement, in USD.
     * @param total {String} The amount of USD the user must keep available in their Global Account.
     */
    constructor(completedPac, outstandingLai, estimatedFees, borrowedPortion, floor, total) { 
        
        CashReserveBreakdown.initialize(this, completedPac, outstandingLai, estimatedFees, borrowedPortion, floor, total);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, completedPac, outstandingLai, estimatedFees, borrowedPortion, floor, total) { 
        obj['completed_pac'] = completedPac;
        obj['outstanding_lai'] = outstandingLai;
        obj['estimated_fees'] = estimatedFees;
        obj['borrowed_portion'] = borrowedPortion;
        obj['floor'] = floor;
        obj['total'] = total;
    }

    /**
     * Constructs a <code>CashReserveBreakdown</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CashReserveBreakdown} obj Optional instance to populate.
     * @return {module:model/CashReserveBreakdown} The populated <code>CashReserveBreakdown</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CashReserveBreakdown();

            if (data.hasOwnProperty('completed_pac')) {
                obj['completed_pac'] = ApiClient.convertToType(data['completed_pac'], 'String');
            }
            if (data.hasOwnProperty('outstanding_lai')) {
                obj['outstanding_lai'] = ApiClient.convertToType(data['outstanding_lai'], 'String');
            }
            if (data.hasOwnProperty('estimated_fees')) {
                obj['estimated_fees'] = ApiClient.convertToType(data['estimated_fees'], 'String');
            }
            if (data.hasOwnProperty('borrowed_portion')) {
                obj['borrowed_portion'] = ApiClient.convertToType(data['borrowed_portion'], 'String');
            }
            if (data.hasOwnProperty('floor')) {
                obj['floor'] = ApiClient.convertToType(data['floor'], 'String');
            }
            if (data.hasOwnProperty('total')) {
                obj['total'] = ApiClient.convertToType(data['total'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CashReserveBreakdown</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CashReserveBreakdown</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CashReserveBreakdown.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['completed_pac'] && !(typeof data['completed_pac'] === 'string' || data['completed_pac'] instanceof String)) {
            throw new Error("Expected the field `completed_pac` to be a primitive type in the JSON string but got " + data['completed_pac']);
        }
        // ensure the json data is a string
        if (data['outstanding_lai'] && !(typeof data['outstanding_lai'] === 'string' || data['outstanding_lai'] instanceof String)) {
            throw new Error("Expected the field `outstanding_lai` to be a primitive type in the JSON string but got " + data['outstanding_lai']);
        }
        // ensure the json data is a string
        if (data['estimated_fees'] && !(typeof data['estimated_fees'] === 'string' || data['estimated_fees'] instanceof String)) {
            throw new Error("Expected the field `estimated_fees` to be a primitive type in the JSON string but got " + data['estimated_fees']);
        }
        // ensure the json data is a string
        if (data['borrowed_portion'] && !(typeof data['borrowed_portion'] === 'string' || data['borrowed_portion'] instanceof String)) {
            throw new Error("Expected the field `borrowed_portion` to be a primitive type in the JSON string but got " + data['borrowed_portion']);
        }
        // ensure the json data is a string
        if (data['floor'] && !(typeof data['floor'] === 'string' || data['floor'] instanceof String)) {
            throw new Error("Expected the field `floor` to be a primitive type in the JSON string but got " + data['floor']);
        }
        // ensure the json data is a string
        if (data['total'] && !(typeof data['total'] === 'string' || data['total'] instanceof String)) {
            throw new Error("Expected the field `total` to be a primitive type in the JSON string but got " + data['total']);
        }

        return true;
    }


}

CashReserveBreakdown.RequiredProperties = ["completed_pac", "outstanding_lai", "estimated_fees", "borrowed_portion", "floor", "total"];

/**
 * Completed PAC (partially accrued coupon) obligations the user owes, in USD.
 * @member {String} completed_pac
 */
CashReserveBreakdown.prototype['completed_pac'] = undefined;

/**
 * Outstanding LAI (leverage accrued interest) the user owes, in USD.
 * @member {String} outstanding_lai
 */
CashReserveBreakdown.prototype['outstanding_lai'] = undefined;

/**
 * Estimated trading fees for the current settlement period, capped at a configured fraction (1% by default) of the user's traded USD volume since 00:00:00 UTC.
 * @member {String} estimated_fees
 */
CashReserveBreakdown.prototype['estimated_fees'] = undefined;

/**
 * Configured fraction (10% by default) of the user's total outstanding borrowed value, in USD.
 * @member {String} borrowed_portion
 */
CashReserveBreakdown.prototype['borrowed_portion'] = undefined;

/**
 * Configured absolute minimum requirement, in USD.
 * @member {String} floor
 */
CashReserveBreakdown.prototype['floor'] = undefined;

/**
 * The amount of USD the user must keep available in their Global Account.
 * @member {String} total
 */
CashReserveBreakdown.prototype['total'] = undefined;






export default CashReserveBreakdown;

