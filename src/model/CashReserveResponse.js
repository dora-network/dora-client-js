/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CashReserveBreakdown from './CashReserveBreakdown';

/**
 * The CashReserveResponse model module.
 * @module model/CashReserveResponse
 * @version 1.0.0
 */
class CashReserveResponse {
    /**
     * Constructs a new <code>CashReserveResponse</code>.
     * @alias module:model/CashReserveResponse
     * @param enforced {Boolean} Whether the minimum cash reserve guard is active in this environment.
     * @param availableUsd {String} The user's currently available USD balance in their Global Account.
     * @param committedUsd {String} USD still counted in available_usd but already claimed by the user's open market buy orders on the Global Account, which reserve no funds at submission time. The reserve is evaluated against available_usd minus committed_usd.
     * @param requiredUsd {String} The user's minimum USD cash reserve requirement.
     * @param satisfied {Boolean} Whether available_usd minus committed_usd is at least required_usd.
     * @param breakdown {module:model/CashReserveBreakdown} 
     */
    constructor(enforced, availableUsd, committedUsd, requiredUsd, satisfied, breakdown) { 
        
        CashReserveResponse.initialize(this, enforced, availableUsd, committedUsd, requiredUsd, satisfied, breakdown);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, enforced, availableUsd, committedUsd, requiredUsd, satisfied, breakdown) { 
        obj['enforced'] = enforced;
        obj['available_usd'] = availableUsd;
        obj['committed_usd'] = committedUsd;
        obj['required_usd'] = requiredUsd;
        obj['satisfied'] = satisfied;
        obj['breakdown'] = breakdown;
    }

    /**
     * Constructs a <code>CashReserveResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CashReserveResponse} obj Optional instance to populate.
     * @return {module:model/CashReserveResponse} The populated <code>CashReserveResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CashReserveResponse();

            if (data.hasOwnProperty('enforced')) {
                obj['enforced'] = ApiClient.convertToType(data['enforced'], 'Boolean');
            }
            if (data.hasOwnProperty('available_usd')) {
                obj['available_usd'] = ApiClient.convertToType(data['available_usd'], 'String');
            }
            if (data.hasOwnProperty('committed_usd')) {
                obj['committed_usd'] = ApiClient.convertToType(data['committed_usd'], 'String');
            }
            if (data.hasOwnProperty('required_usd')) {
                obj['required_usd'] = ApiClient.convertToType(data['required_usd'], 'String');
            }
            if (data.hasOwnProperty('satisfied')) {
                obj['satisfied'] = ApiClient.convertToType(data['satisfied'], 'Boolean');
            }
            if (data.hasOwnProperty('breakdown')) {
                obj['breakdown'] = CashReserveBreakdown.constructFromObject(data['breakdown']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CashReserveResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CashReserveResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CashReserveResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['available_usd'] && !(typeof data['available_usd'] === 'string' || data['available_usd'] instanceof String)) {
            throw new Error("Expected the field `available_usd` to be a primitive type in the JSON string but got " + data['available_usd']);
        }
        // ensure the json data is a string
        if (data['committed_usd'] && !(typeof data['committed_usd'] === 'string' || data['committed_usd'] instanceof String)) {
            throw new Error("Expected the field `committed_usd` to be a primitive type in the JSON string but got " + data['committed_usd']);
        }
        // ensure the json data is a string
        if (data['required_usd'] && !(typeof data['required_usd'] === 'string' || data['required_usd'] instanceof String)) {
            throw new Error("Expected the field `required_usd` to be a primitive type in the JSON string but got " + data['required_usd']);
        }
        // validate the optional field `breakdown`
        if (data['breakdown']) { // data not null
          CashReserveBreakdown.validateJSON(data['breakdown']);
        }

        return true;
    }


}

CashReserveResponse.RequiredProperties = ["enforced", "available_usd", "committed_usd", "required_usd", "satisfied", "breakdown"];

/**
 * Whether the minimum cash reserve guard is active in this environment.
 * @member {Boolean} enforced
 */
CashReserveResponse.prototype['enforced'] = undefined;

/**
 * The user's currently available USD balance in their Global Account.
 * @member {String} available_usd
 */
CashReserveResponse.prototype['available_usd'] = undefined;

/**
 * USD still counted in available_usd but already claimed by the user's open market buy orders on the Global Account, which reserve no funds at submission time. The reserve is evaluated against available_usd minus committed_usd.
 * @member {String} committed_usd
 */
CashReserveResponse.prototype['committed_usd'] = undefined;

/**
 * The user's minimum USD cash reserve requirement.
 * @member {String} required_usd
 */
CashReserveResponse.prototype['required_usd'] = undefined;

/**
 * Whether available_usd minus committed_usd is at least required_usd.
 * @member {Boolean} satisfied
 */
CashReserveResponse.prototype['satisfied'] = undefined;

/**
 * @member {module:model/CashReserveBreakdown} breakdown
 */
CashReserveResponse.prototype['breakdown'] = undefined;






export default CashReserveResponse;

