/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateAffiliateProgramRequest model module.
 * @module model/CreateAffiliateProgramRequest
 * @version 1.0.0
 */
class CreateAffiliateProgramRequest {
    /**
     * Constructs a new <code>CreateAffiliateProgramRequest</code>.
     * @alias module:model/CreateAffiliateProgramRequest
     * @param tenantId {String} Owning tenant for administration. This does not define a referral destination.
     * @param name {String} No surrounding whitespace or control characters.
     */
    constructor(tenantId, name) { 
        
        CreateAffiliateProgramRequest.initialize(this, tenantId, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, tenantId, name) { 
        obj['tenant_id'] = tenantId;
        obj['name'] = name;
        obj['description'] = '';
        obj['is_active'] = false;
    }

    /**
     * Constructs a <code>CreateAffiliateProgramRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateAffiliateProgramRequest} obj Optional instance to populate.
     * @return {module:model/CreateAffiliateProgramRequest} The populated <code>CreateAffiliateProgramRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateAffiliateProgramRequest();

            if (data.hasOwnProperty('tenant_id')) {
                obj['tenant_id'] = ApiClient.convertToType(data['tenant_id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('is_active')) {
                obj['is_active'] = ApiClient.convertToType(data['is_active'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateAffiliateProgramRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateAffiliateProgramRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateAffiliateProgramRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['tenant_id'] && !(typeof data['tenant_id'] === 'string' || data['tenant_id'] instanceof String)) {
            throw new Error("Expected the field `tenant_id` to be a primitive type in the JSON string but got " + data['tenant_id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }

        return true;
    }


}

CreateAffiliateProgramRequest.RequiredProperties = ["tenant_id", "name"];

/**
 * Owning tenant for administration. This does not define a referral destination.
 * @member {String} tenant_id
 */
CreateAffiliateProgramRequest.prototype['tenant_id'] = undefined;

/**
 * No surrounding whitespace or control characters.
 * @member {String} name
 */
CreateAffiliateProgramRequest.prototype['name'] = undefined;

/**
 * Omitted or null defaults to an empty description.
 * @member {String} description
 * @default ''
 */
CreateAffiliateProgramRequest.prototype['description'] = '';

/**
 * Inactive programs retain registrations, but cannot issue or resolve codes. Set true to create an active program.
 * @member {Boolean} is_active
 * @default false
 */
CreateAffiliateProgramRequest.prototype['is_active'] = false;






export default CreateAffiliateProgramRequest;

