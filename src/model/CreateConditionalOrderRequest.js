/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateConditionalOrderRequest model module.
 * @module model/CreateConditionalOrderRequest
 * @version 1.0.0
 */
class CreateConditionalOrderRequest {
    /**
     * Constructs a new <code>CreateConditionalOrderRequest</code>.
     * @alias module:model/CreateConditionalOrderRequest
     * @param price {String} 
     * @param orderBookId {String} Required: the order book to submit the order to
     * @param positionId {String} Required: the position to submit the order to
     * @param assetId {String} Required: the asset to submit the order to
     */
    constructor(price, orderBookId, positionId, assetId) { 
        
        CreateConditionalOrderRequest.initialize(this, price, orderBookId, positionId, assetId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, price, orderBookId, positionId, assetId) { 
        obj['price'] = price;
        obj['order_book_id'] = orderBookId;
        obj['position_id'] = positionId;
        obj['asset_id'] = assetId;
    }

    /**
     * Constructs a <code>CreateConditionalOrderRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateConditionalOrderRequest} obj Optional instance to populate.
     * @return {module:model/CreateConditionalOrderRequest} The populated <code>CreateConditionalOrderRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateConditionalOrderRequest();

            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'String');
            }
            if (data.hasOwnProperty('order_book_id')) {
                obj['order_book_id'] = ApiClient.convertToType(data['order_book_id'], 'String');
            }
            if (data.hasOwnProperty('position_id')) {
                obj['position_id'] = ApiClient.convertToType(data['position_id'], 'String');
            }
            if (data.hasOwnProperty('asset_id')) {
                obj['asset_id'] = ApiClient.convertToType(data['asset_id'], 'String');
            }
            if (data.hasOwnProperty('stop_loss_price')) {
                obj['stop_loss_price'] = ApiClient.convertToType(data['stop_loss_price'], 'String');
            }
            if (data.hasOwnProperty('take_profit_price')) {
                obj['take_profit_price'] = ApiClient.convertToType(data['take_profit_price'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateConditionalOrderRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateConditionalOrderRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateConditionalOrderRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['price'] && !(typeof data['price'] === 'string' || data['price'] instanceof String)) {
            throw new Error("Expected the field `price` to be a primitive type in the JSON string but got " + data['price']);
        }
        // ensure the json data is a string
        if (data['order_book_id'] && !(typeof data['order_book_id'] === 'string' || data['order_book_id'] instanceof String)) {
            throw new Error("Expected the field `order_book_id` to be a primitive type in the JSON string but got " + data['order_book_id']);
        }
        // ensure the json data is a string
        if (data['position_id'] && !(typeof data['position_id'] === 'string' || data['position_id'] instanceof String)) {
            throw new Error("Expected the field `position_id` to be a primitive type in the JSON string but got " + data['position_id']);
        }
        // ensure the json data is a string
        if (data['asset_id'] && !(typeof data['asset_id'] === 'string' || data['asset_id'] instanceof String)) {
            throw new Error("Expected the field `asset_id` to be a primitive type in the JSON string but got " + data['asset_id']);
        }
        // ensure the json data is a string
        if (data['stop_loss_price'] && !(typeof data['stop_loss_price'] === 'string' || data['stop_loss_price'] instanceof String)) {
            throw new Error("Expected the field `stop_loss_price` to be a primitive type in the JSON string but got " + data['stop_loss_price']);
        }
        // ensure the json data is a string
        if (data['take_profit_price'] && !(typeof data['take_profit_price'] === 'string' || data['take_profit_price'] instanceof String)) {
            throw new Error("Expected the field `take_profit_price` to be a primitive type in the JSON string but got " + data['take_profit_price']);
        }

        return true;
    }


}

CreateConditionalOrderRequest.RequiredProperties = ["price", "order_book_id", "position_id", "asset_id"];

/**
 * @member {String} price
 */
CreateConditionalOrderRequest.prototype['price'] = undefined;

/**
 * Required: the order book to submit the order to
 * @member {String} order_book_id
 */
CreateConditionalOrderRequest.prototype['order_book_id'] = undefined;

/**
 * Required: the position to submit the order to
 * @member {String} position_id
 */
CreateConditionalOrderRequest.prototype['position_id'] = undefined;

/**
 * Required: the asset to submit the order to
 * @member {String} asset_id
 */
CreateConditionalOrderRequest.prototype['asset_id'] = undefined;

/**
 * Stop loss price
 * @member {String} stop_loss_price
 */
CreateConditionalOrderRequest.prototype['stop_loss_price'] = undefined;

/**
 * Take profit price
 * @member {String} take_profit_price
 */
CreateConditionalOrderRequest.prototype['take_profit_price'] = undefined;






export default CreateConditionalOrderRequest;

