/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import OrderKind from './OrderKind';
import OrderModifierKind from './OrderModifierKind';
import Side from './Side';

/**
 * The CreateOrderRequest model module.
 * @module model/CreateOrderRequest
 * @version 1.0.0
 */
class CreateOrderRequest {
    /**
     * Constructs a new <code>CreateOrderRequest</code>.
     * @alias module:model/CreateOrderRequest
     * @param quantity {String} 
     * @param inverseLeverage {String} 
     * @param kind {module:model/OrderKind} 
     * @param side {module:model/Side} Required: Must be either 'BUY' or 'SELL'
     * @param fromGlobalPosition {Boolean} use global position for the order or isolated. required.
     * @param orderBookId {String} Required: the order book to submit the order to
     */
    constructor(quantity, inverseLeverage, kind, side, fromGlobalPosition, orderBookId) { 
        
        CreateOrderRequest.initialize(this, quantity, inverseLeverage, kind, side, fromGlobalPosition, orderBookId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, quantity, inverseLeverage, kind, side, fromGlobalPosition, orderBookId) { 
        obj['quantity'] = quantity;
        obj['inverse_leverage'] = inverseLeverage;
        obj['kind'] = kind;
        obj['side'] = side;
        obj['from_global_position'] = fromGlobalPosition;
        obj['order_book_id'] = orderBookId;
    }

    /**
     * Constructs a <code>CreateOrderRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateOrderRequest} obj Optional instance to populate.
     * @return {module:model/CreateOrderRequest} The populated <code>CreateOrderRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateOrderRequest();

            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'String');
            }
            if (data.hasOwnProperty('inverse_leverage')) {
                obj['inverse_leverage'] = ApiClient.convertToType(data['inverse_leverage'], 'String');
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = OrderKind.constructFromObject(data['kind']);
            }
            if (data.hasOwnProperty('side')) {
                obj['side'] = Side.constructFromObject(data['side']);
            }
            if (data.hasOwnProperty('from_global_position')) {
                obj['from_global_position'] = ApiClient.convertToType(data['from_global_position'], 'Boolean');
            }
            if (data.hasOwnProperty('order_book_id')) {
                obj['order_book_id'] = ApiClient.convertToType(data['order_book_id'], 'String');
            }
            if (data.hasOwnProperty('order_modifiers')) {
                obj['order_modifiers'] = ApiClient.convertToType(data['order_modifiers'], [OrderModifierKind]);
            }
            if (data.hasOwnProperty('good_till_date')) {
                obj['good_till_date'] = ApiClient.convertToType(data['good_till_date'], 'Date');
            }
            if (data.hasOwnProperty('client_order_id')) {
                obj['client_order_id'] = ApiClient.convertToType(data['client_order_id'], 'String');
            }
            if (data.hasOwnProperty('stop_loss_price')) {
                obj['stop_loss_price'] = ApiClient.convertToType(data['stop_loss_price'], 'String');
            }
            if (data.hasOwnProperty('take_profit_price')) {
                obj['take_profit_price'] = ApiClient.convertToType(data['take_profit_price'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateOrderRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateOrderRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateOrderRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['quantity'] && !(typeof data['quantity'] === 'string' || data['quantity'] instanceof String)) {
            throw new Error("Expected the field `quantity` to be a primitive type in the JSON string but got " + data['quantity']);
        }
        // ensure the json data is a string
        if (data['inverse_leverage'] && !(typeof data['inverse_leverage'] === 'string' || data['inverse_leverage'] instanceof String)) {
            throw new Error("Expected the field `inverse_leverage` to be a primitive type in the JSON string but got " + data['inverse_leverage']);
        }
        // ensure the json data is a string
        if (data['price'] && !(typeof data['price'] === 'string' || data['price'] instanceof String)) {
            throw new Error("Expected the field `price` to be a primitive type in the JSON string but got " + data['price']);
        }
        // ensure the json data is a string
        if (data['order_book_id'] && !(typeof data['order_book_id'] === 'string' || data['order_book_id'] instanceof String)) {
            throw new Error("Expected the field `order_book_id` to be a primitive type in the JSON string but got " + data['order_book_id']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['order_modifiers'])) {
            throw new Error("Expected the field `order_modifiers` to be an array in the JSON data but got " + data['order_modifiers']);
        }
        // ensure the json data is a string
        if (data['client_order_id'] && !(typeof data['client_order_id'] === 'string' || data['client_order_id'] instanceof String)) {
            throw new Error("Expected the field `client_order_id` to be a primitive type in the JSON string but got " + data['client_order_id']);
        }
        // ensure the json data is a string
        if (data['stop_loss_price'] && !(typeof data['stop_loss_price'] === 'string' || data['stop_loss_price'] instanceof String)) {
            throw new Error("Expected the field `stop_loss_price` to be a primitive type in the JSON string but got " + data['stop_loss_price']);
        }
        // ensure the json data is a string
        if (data['take_profit_price'] && !(typeof data['take_profit_price'] === 'string' || data['take_profit_price'] instanceof String)) {
            throw new Error("Expected the field `take_profit_price` to be a primitive type in the JSON string but got " + data['take_profit_price']);
        }

        return true;
    }


}

CreateOrderRequest.RequiredProperties = ["quantity", "inverse_leverage", "kind", "side", "from_global_position", "order_book_id"];

/**
 * @member {String} quantity
 */
CreateOrderRequest.prototype['quantity'] = undefined;

/**
 * @member {String} inverse_leverage
 */
CreateOrderRequest.prototype['inverse_leverage'] = undefined;

/**
 * @member {String} price
 */
CreateOrderRequest.prototype['price'] = undefined;

/**
 * @member {module:model/OrderKind} kind
 */
CreateOrderRequest.prototype['kind'] = undefined;

/**
 * Required: Must be either 'BUY' or 'SELL'
 * @member {module:model/Side} side
 */
CreateOrderRequest.prototype['side'] = undefined;

/**
 * use global position for the order or isolated. required.
 * @member {Boolean} from_global_position
 */
CreateOrderRequest.prototype['from_global_position'] = undefined;

/**
 * Required: the order book to submit the order to
 * @member {String} order_book_id
 */
CreateOrderRequest.prototype['order_book_id'] = undefined;

/**
 * @member {Array.<module:model/OrderModifierKind>} order_modifiers
 */
CreateOrderRequest.prototype['order_modifiers'] = undefined;

/**
 * @member {Date} good_till_date
 */
CreateOrderRequest.prototype['good_till_date'] = undefined;

/**
 * An optional client-provided identifier for the order.
 * @member {String} client_order_id
 */
CreateOrderRequest.prototype['client_order_id'] = undefined;

/**
 * Stop loss price
 * @member {String} stop_loss_price
 */
CreateOrderRequest.prototype['stop_loss_price'] = undefined;

/**
 * Take profit price
 * @member {String} take_profit_price
 */
CreateOrderRequest.prototype['take_profit_price'] = undefined;






export default CreateOrderRequest;

