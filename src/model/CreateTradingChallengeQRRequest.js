/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateTradingChallengeQRRequest model module.
 * @module model/CreateTradingChallengeQRRequest
 * @version 1.0.0
 */
class CreateTradingChallengeQRRequest {
    /**
     * Constructs a new <code>CreateTradingChallengeQRRequest</code>.
     * @alias module:model/CreateTradingChallengeQRRequest
     * @param minRewardAmount {String} Must be greater than zero and greater than initial_user_balance.
     * @param maxRewardAmount {String} Must be greater than or equal to min_reward_amount.
     */
    constructor(minRewardAmount, maxRewardAmount) { 
        
        CreateTradingChallengeQRRequest.initialize(this, minRewardAmount, maxRewardAmount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, minRewardAmount, maxRewardAmount) { 
        obj['min_reward_amount'] = minRewardAmount;
        obj['max_reward_amount'] = maxRewardAmount;
        obj['reward_claim_grace_days'] = 14;
    }

    /**
     * Constructs a <code>CreateTradingChallengeQRRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateTradingChallengeQRRequest} obj Optional instance to populate.
     * @return {module:model/CreateTradingChallengeQRRequest} The populated <code>CreateTradingChallengeQRRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateTradingChallengeQRRequest();

            if (data.hasOwnProperty('min_reward_amount')) {
                obj['min_reward_amount'] = ApiClient.convertToType(data['min_reward_amount'], 'String');
            }
            if (data.hasOwnProperty('max_reward_amount')) {
                obj['max_reward_amount'] = ApiClient.convertToType(data['max_reward_amount'], 'String');
            }
            if (data.hasOwnProperty('reward_claim_grace_days')) {
                obj['reward_claim_grace_days'] = ApiClient.convertToType(data['reward_claim_grace_days'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateTradingChallengeQRRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateTradingChallengeQRRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateTradingChallengeQRRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['min_reward_amount'] && !(typeof data['min_reward_amount'] === 'string' || data['min_reward_amount'] instanceof String)) {
            throw new Error("Expected the field `min_reward_amount` to be a primitive type in the JSON string but got " + data['min_reward_amount']);
        }
        // ensure the json data is a string
        if (data['max_reward_amount'] && !(typeof data['max_reward_amount'] === 'string' || data['max_reward_amount'] instanceof String)) {
            throw new Error("Expected the field `max_reward_amount` to be a primitive type in the JSON string but got " + data['max_reward_amount']);
        }

        return true;
    }


}

CreateTradingChallengeQRRequest.RequiredProperties = ["min_reward_amount", "max_reward_amount"];

/**
 * Must be greater than zero and greater than initial_user_balance.
 * @member {String} min_reward_amount
 */
CreateTradingChallengeQRRequest.prototype['min_reward_amount'] = undefined;

/**
 * Must be greater than or equal to min_reward_amount.
 * @member {String} max_reward_amount
 */
CreateTradingChallengeQRRequest.prototype['max_reward_amount'] = undefined;

/**
 * @member {Number} reward_claim_grace_days
 * @default 14
 */
CreateTradingChallengeQRRequest.prototype['reward_claim_grace_days'] = 14;






export default CreateTradingChallengeQRRequest;

