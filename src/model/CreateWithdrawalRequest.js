/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateWithdrawalRequest model module.
 * @module model/CreateWithdrawalRequest
 * @version 1.0.0
 */
class CreateWithdrawalRequest {
    /**
     * Constructs a new <code>CreateWithdrawalRequest</code>.
     * Request to create a USDC withdrawal. No fee quote is required: creating the request reserves the quantity only, and the withdrawal&#39;s fee is quoted and locked later, as part of approval.
     * @alias module:model/CreateWithdrawalRequest
     * @param withdrawalId {String} Client-supplied idempotency key (also the on-chain correlation key). Repeating a request with the same withdrawal_id has no additional effect.
     * @param toAddress {String} Destination wallet address as a 0x-prefixed hex string. Must not be the zero address.
     * @param quantity {String} Human-decimal USDC quantity to withdraw. Must be positive and no finer than USDC's 6 on-chain decimals.
     */
    constructor(withdrawalId, toAddress, quantity) { 
        
        CreateWithdrawalRequest.initialize(this, withdrawalId, toAddress, quantity);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, withdrawalId, toAddress, quantity) { 
        obj['withdrawal_id'] = withdrawalId;
        obj['to_address'] = toAddress;
        obj['quantity'] = quantity;
    }

    /**
     * Constructs a <code>CreateWithdrawalRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateWithdrawalRequest} obj Optional instance to populate.
     * @return {module:model/CreateWithdrawalRequest} The populated <code>CreateWithdrawalRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateWithdrawalRequest();

            if (data.hasOwnProperty('withdrawal_id')) {
                obj['withdrawal_id'] = ApiClient.convertToType(data['withdrawal_id'], 'String');
            }
            if (data.hasOwnProperty('to_address')) {
                obj['to_address'] = ApiClient.convertToType(data['to_address'], 'String');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateWithdrawalRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateWithdrawalRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateWithdrawalRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['withdrawal_id'] && !(typeof data['withdrawal_id'] === 'string' || data['withdrawal_id'] instanceof String)) {
            throw new Error("Expected the field `withdrawal_id` to be a primitive type in the JSON string but got " + data['withdrawal_id']);
        }
        // ensure the json data is a string
        if (data['to_address'] && !(typeof data['to_address'] === 'string' || data['to_address'] instanceof String)) {
            throw new Error("Expected the field `to_address` to be a primitive type in the JSON string but got " + data['to_address']);
        }
        // ensure the json data is a string
        if (data['quantity'] && !(typeof data['quantity'] === 'string' || data['quantity'] instanceof String)) {
            throw new Error("Expected the field `quantity` to be a primitive type in the JSON string but got " + data['quantity']);
        }

        return true;
    }


}

CreateWithdrawalRequest.RequiredProperties = ["withdrawal_id", "to_address", "quantity"];

/**
 * Client-supplied idempotency key (also the on-chain correlation key). Repeating a request with the same withdrawal_id has no additional effect.
 * @member {String} withdrawal_id
 */
CreateWithdrawalRequest.prototype['withdrawal_id'] = undefined;

/**
 * Destination wallet address as a 0x-prefixed hex string. Must not be the zero address.
 * @member {String} to_address
 */
CreateWithdrawalRequest.prototype['to_address'] = undefined;

/**
 * Human-decimal USDC quantity to withdraw. Must be positive and no finer than USDC's 6 on-chain decimals.
 * @member {String} quantity
 */
CreateWithdrawalRequest.prototype['quantity'] = undefined;






export default CreateWithdrawalRequest;

