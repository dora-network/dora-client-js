/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CurrentLeverageAccruedInterest model module.
 * @module model/CurrentLeverageAccruedInterest
 * @version 1.0.0
 */
class CurrentLeverageAccruedInterest {
    /**
     * Constructs a new <code>CurrentLeverageAccruedInterest</code>.
     * @alias module:model/CurrentLeverageAccruedInterest
     * @param userId {String} 
     * @param assetId {String} 
     * @param positionId {String} 
     * @param currentAccruedInterestUsd {String} 
     */
    constructor(userId, assetId, positionId, currentAccruedInterestUsd) { 
        
        CurrentLeverageAccruedInterest.initialize(this, userId, assetId, positionId, currentAccruedInterestUsd);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, userId, assetId, positionId, currentAccruedInterestUsd) { 
        obj['user_id'] = userId;
        obj['asset_id'] = assetId;
        obj['position_id'] = positionId;
        obj['current_accrued_interest_usd'] = currentAccruedInterestUsd;
    }

    /**
     * Constructs a <code>CurrentLeverageAccruedInterest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CurrentLeverageAccruedInterest} obj Optional instance to populate.
     * @return {module:model/CurrentLeverageAccruedInterest} The populated <code>CurrentLeverageAccruedInterest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CurrentLeverageAccruedInterest();

            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('asset_id')) {
                obj['asset_id'] = ApiClient.convertToType(data['asset_id'], 'String');
            }
            if (data.hasOwnProperty('position_id')) {
                obj['position_id'] = ApiClient.convertToType(data['position_id'], 'String');
            }
            if (data.hasOwnProperty('current_accrued_interest_usd')) {
                obj['current_accrued_interest_usd'] = ApiClient.convertToType(data['current_accrued_interest_usd'], 'String');
            }
            if (data.hasOwnProperty('asset_name')) {
                obj['asset_name'] = ApiClient.convertToType(data['asset_name'], 'String');
            }
            if (data.hasOwnProperty('asset_symbol')) {
                obj['asset_symbol'] = ApiClient.convertToType(data['asset_symbol'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CurrentLeverageAccruedInterest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CurrentLeverageAccruedInterest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CurrentLeverageAccruedInterest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['asset_id'] && !(typeof data['asset_id'] === 'string' || data['asset_id'] instanceof String)) {
            throw new Error("Expected the field `asset_id` to be a primitive type in the JSON string but got " + data['asset_id']);
        }
        // ensure the json data is a string
        if (data['position_id'] && !(typeof data['position_id'] === 'string' || data['position_id'] instanceof String)) {
            throw new Error("Expected the field `position_id` to be a primitive type in the JSON string but got " + data['position_id']);
        }
        // ensure the json data is a string
        if (data['current_accrued_interest_usd'] && !(typeof data['current_accrued_interest_usd'] === 'string' || data['current_accrued_interest_usd'] instanceof String)) {
            throw new Error("Expected the field `current_accrued_interest_usd` to be a primitive type in the JSON string but got " + data['current_accrued_interest_usd']);
        }
        // ensure the json data is a string
        if (data['asset_name'] && !(typeof data['asset_name'] === 'string' || data['asset_name'] instanceof String)) {
            throw new Error("Expected the field `asset_name` to be a primitive type in the JSON string but got " + data['asset_name']);
        }
        // ensure the json data is a string
        if (data['asset_symbol'] && !(typeof data['asset_symbol'] === 'string' || data['asset_symbol'] instanceof String)) {
            throw new Error("Expected the field `asset_symbol` to be a primitive type in the JSON string but got " + data['asset_symbol']);
        }

        return true;
    }


}

CurrentLeverageAccruedInterest.RequiredProperties = ["user_id", "asset_id", "position_id", "current_accrued_interest_usd"];

/**
 * @member {String} user_id
 */
CurrentLeverageAccruedInterest.prototype['user_id'] = undefined;

/**
 * @member {String} asset_id
 */
CurrentLeverageAccruedInterest.prototype['asset_id'] = undefined;

/**
 * @member {String} position_id
 */
CurrentLeverageAccruedInterest.prototype['position_id'] = undefined;

/**
 * @member {String} current_accrued_interest_usd
 */
CurrentLeverageAccruedInterest.prototype['current_accrued_interest_usd'] = undefined;

/**
 * @member {String} asset_name
 */
CurrentLeverageAccruedInterest.prototype['asset_name'] = undefined;

/**
 * @member {String} asset_symbol
 */
CurrentLeverageAccruedInterest.prototype['asset_symbol'] = undefined;






export default CurrentLeverageAccruedInterest;

