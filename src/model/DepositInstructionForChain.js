/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DepositCall from './DepositCall';
import PermitTypedData from './PermitTypedData';

/**
 * The DepositInstructionForChain model module.
 * @module model/DepositInstructionForChain
 * @version 1.0.0
 */
class DepositInstructionForChain {
    /**
     * Constructs a new <code>DepositInstructionForChain</code>.
     * Everything the caller needs to deposit USDC into the Dora vault on a single chain with one signature and one transaction.
     * @alias module:model/DepositInstructionForChain
     * @param networkName {String} Human-readable network name, e.g. 'Ethereum Mainnet'.
     * @param chainId {String} EVM chain ID.
     * @param contractAddress {String} The DoraUSDCVault contract address for this chain, as a 0x-prefixed hex string.
     * @param usdcAddress {String} The ERC-20 USDC token contract on this chain, as a 0x-prefixed hex string. It is the verifying contract of the permit; the spender granted by the permit is contract_address (the vault).
     * @param approval {module:model/PermitTypedData} 
     * @param deposit {module:model/DepositCall} 
     */
    constructor(networkName, chainId, contractAddress, usdcAddress, approval, deposit) { 
        
        DepositInstructionForChain.initialize(this, networkName, chainId, contractAddress, usdcAddress, approval, deposit);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, networkName, chainId, contractAddress, usdcAddress, approval, deposit) { 
        obj['network_name'] = networkName;
        obj['chain_id'] = chainId;
        obj['contract_address'] = contractAddress;
        obj['usdc_address'] = usdcAddress;
        obj['approval'] = approval;
        obj['deposit'] = deposit;
    }

    /**
     * Constructs a <code>DepositInstructionForChain</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DepositInstructionForChain} obj Optional instance to populate.
     * @return {module:model/DepositInstructionForChain} The populated <code>DepositInstructionForChain</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DepositInstructionForChain();

            if (data.hasOwnProperty('network_name')) {
                obj['network_name'] = ApiClient.convertToType(data['network_name'], 'String');
            }
            if (data.hasOwnProperty('chain_id')) {
                obj['chain_id'] = ApiClient.convertToType(data['chain_id'], 'String');
            }
            if (data.hasOwnProperty('contract_address')) {
                obj['contract_address'] = ApiClient.convertToType(data['contract_address'], 'String');
            }
            if (data.hasOwnProperty('usdc_address')) {
                obj['usdc_address'] = ApiClient.convertToType(data['usdc_address'], 'String');
            }
            if (data.hasOwnProperty('approval')) {
                obj['approval'] = PermitTypedData.constructFromObject(data['approval']);
            }
            if (data.hasOwnProperty('deposit')) {
                obj['deposit'] = DepositCall.constructFromObject(data['deposit']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DepositInstructionForChain</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DepositInstructionForChain</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DepositInstructionForChain.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['network_name'] && !(typeof data['network_name'] === 'string' || data['network_name'] instanceof String)) {
            throw new Error("Expected the field `network_name` to be a primitive type in the JSON string but got " + data['network_name']);
        }
        // ensure the json data is a string
        if (data['chain_id'] && !(typeof data['chain_id'] === 'string' || data['chain_id'] instanceof String)) {
            throw new Error("Expected the field `chain_id` to be a primitive type in the JSON string but got " + data['chain_id']);
        }
        // ensure the json data is a string
        if (data['contract_address'] && !(typeof data['contract_address'] === 'string' || data['contract_address'] instanceof String)) {
            throw new Error("Expected the field `contract_address` to be a primitive type in the JSON string but got " + data['contract_address']);
        }
        // ensure the json data is a string
        if (data['usdc_address'] && !(typeof data['usdc_address'] === 'string' || data['usdc_address'] instanceof String)) {
            throw new Error("Expected the field `usdc_address` to be a primitive type in the JSON string but got " + data['usdc_address']);
        }
        // validate the optional field `approval`
        if (data['approval']) { // data not null
          PermitTypedData.validateJSON(data['approval']);
        }
        // validate the optional field `deposit`
        if (data['deposit']) { // data not null
          DepositCall.validateJSON(data['deposit']);
        }

        return true;
    }


}

DepositInstructionForChain.RequiredProperties = ["network_name", "chain_id", "contract_address", "usdc_address", "approval", "deposit"];

/**
 * Human-readable network name, e.g. 'Ethereum Mainnet'.
 * @member {String} network_name
 */
DepositInstructionForChain.prototype['network_name'] = undefined;

/**
 * EVM chain ID.
 * @member {String} chain_id
 */
DepositInstructionForChain.prototype['chain_id'] = undefined;

/**
 * The DoraUSDCVault contract address for this chain, as a 0x-prefixed hex string.
 * @member {String} contract_address
 */
DepositInstructionForChain.prototype['contract_address'] = undefined;

/**
 * The ERC-20 USDC token contract on this chain, as a 0x-prefixed hex string. It is the verifying contract of the permit; the spender granted by the permit is contract_address (the vault).
 * @member {String} usdc_address
 */
DepositInstructionForChain.prototype['usdc_address'] = undefined;

/**
 * @member {module:model/PermitTypedData} approval
 */
DepositInstructionForChain.prototype['approval'] = undefined;

/**
 * @member {module:model/DepositCall} deposit
 */
DepositInstructionForChain.prototype['deposit'] = undefined;






export default DepositInstructionForChain;

