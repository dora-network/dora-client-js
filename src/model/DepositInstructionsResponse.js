/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DepositInstructionForChain from './DepositInstructionForChain';

/**
 * The DepositInstructionsResponse model module.
 * @module model/DepositInstructionsResponse
 * @version 1.0.0
 */
class DepositInstructionsResponse {
    /**
     * Constructs a new <code>DepositInstructionsResponse</code>.
     * Per-chain instructions for depositing USDC into the Dora vault via EIP-2612 permit.
     * @alias module:model/DepositInstructionsResponse
     * @param userId {String} The authenticated user the instructions are issued for.
     * @param ownerAddress {String} The wallet address the instructions were issued for, echoed from the request.
     * @param quantity {String} Human-decimal USDC deposit quantity, echoed from the request.
     * @param chains {Array.<module:model/DepositInstructionForChain>} 
     */
    constructor(userId, ownerAddress, quantity, chains) { 
        
        DepositInstructionsResponse.initialize(this, userId, ownerAddress, quantity, chains);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, userId, ownerAddress, quantity, chains) { 
        obj['user_id'] = userId;
        obj['owner_address'] = ownerAddress;
        obj['quantity'] = quantity;
        obj['chains'] = chains;
    }

    /**
     * Constructs a <code>DepositInstructionsResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DepositInstructionsResponse} obj Optional instance to populate.
     * @return {module:model/DepositInstructionsResponse} The populated <code>DepositInstructionsResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DepositInstructionsResponse();

            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('owner_address')) {
                obj['owner_address'] = ApiClient.convertToType(data['owner_address'], 'String');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'String');
            }
            if (data.hasOwnProperty('chains')) {
                obj['chains'] = ApiClient.convertToType(data['chains'], [DepositInstructionForChain]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DepositInstructionsResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DepositInstructionsResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of DepositInstructionsResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['owner_address'] && !(typeof data['owner_address'] === 'string' || data['owner_address'] instanceof String)) {
            throw new Error("Expected the field `owner_address` to be a primitive type in the JSON string but got " + data['owner_address']);
        }
        // ensure the json data is a string
        if (data['quantity'] && !(typeof data['quantity'] === 'string' || data['quantity'] instanceof String)) {
            throw new Error("Expected the field `quantity` to be a primitive type in the JSON string but got " + data['quantity']);
        }
        if (data['chains']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['chains'])) {
                throw new Error("Expected the field `chains` to be an array in the JSON data but got " + data['chains']);
            }
            // validate the optional field `chains` (array)
            for (const item of data['chains']) {
                DepositInstructionForChain.validateJSON(item);
            };
        }

        return true;
    }


}

DepositInstructionsResponse.RequiredProperties = ["user_id", "owner_address", "quantity", "chains"];

/**
 * The authenticated user the instructions are issued for.
 * @member {String} user_id
 */
DepositInstructionsResponse.prototype['user_id'] = undefined;

/**
 * The wallet address the instructions were issued for, echoed from the request.
 * @member {String} owner_address
 */
DepositInstructionsResponse.prototype['owner_address'] = undefined;

/**
 * Human-decimal USDC deposit quantity, echoed from the request.
 * @member {String} quantity
 */
DepositInstructionsResponse.prototype['quantity'] = undefined;

/**
 * @member {Array.<module:model/DepositInstructionForChain>} chains
 */
DepositInstructionsResponse.prototype['chains'] = undefined;






export default DepositInstructionsResponse;

