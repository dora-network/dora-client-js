/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The IsolatedPosition model module.
 * @module model/IsolatedPosition
 * @version 1.0.0
 */
class IsolatedPosition {
    /**
     * Constructs a new <code>IsolatedPosition</code>.
     * @alias module:model/IsolatedPosition
     * @param globalPositionId {String} 
     * @param isolatedPositionId {String} 
     * @param transactionId {String} 
     */
    constructor(globalPositionId, isolatedPositionId, transactionId) { 
        
        IsolatedPosition.initialize(this, globalPositionId, isolatedPositionId, transactionId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, globalPositionId, isolatedPositionId, transactionId) { 
        obj['global_position_id'] = globalPositionId;
        obj['isolated_position_id'] = isolatedPositionId;
        obj['transaction_id'] = transactionId;
    }

    /**
     * Constructs a <code>IsolatedPosition</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/IsolatedPosition} obj Optional instance to populate.
     * @return {module:model/IsolatedPosition} The populated <code>IsolatedPosition</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new IsolatedPosition();

            if (data.hasOwnProperty('global_position_id')) {
                obj['global_position_id'] = ApiClient.convertToType(data['global_position_id'], 'String');
            }
            if (data.hasOwnProperty('isolated_position_id')) {
                obj['isolated_position_id'] = ApiClient.convertToType(data['isolated_position_id'], 'String');
            }
            if (data.hasOwnProperty('transaction_id')) {
                obj['transaction_id'] = ApiClient.convertToType(data['transaction_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>IsolatedPosition</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>IsolatedPosition</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of IsolatedPosition.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['global_position_id'] && !(typeof data['global_position_id'] === 'string' || data['global_position_id'] instanceof String)) {
            throw new Error("Expected the field `global_position_id` to be a primitive type in the JSON string but got " + data['global_position_id']);
        }
        // ensure the json data is a string
        if (data['isolated_position_id'] && !(typeof data['isolated_position_id'] === 'string' || data['isolated_position_id'] instanceof String)) {
            throw new Error("Expected the field `isolated_position_id` to be a primitive type in the JSON string but got " + data['isolated_position_id']);
        }
        // ensure the json data is a string
        if (data['transaction_id'] && !(typeof data['transaction_id'] === 'string' || data['transaction_id'] instanceof String)) {
            throw new Error("Expected the field `transaction_id` to be a primitive type in the JSON string but got " + data['transaction_id']);
        }

        return true;
    }


}

IsolatedPosition.RequiredProperties = ["global_position_id", "isolated_position_id", "transaction_id"];

/**
 * @member {String} global_position_id
 */
IsolatedPosition.prototype['global_position_id'] = undefined;

/**
 * @member {String} isolated_position_id
 */
IsolatedPosition.prototype['isolated_position_id'] = undefined;

/**
 * @member {String} transaction_id
 */
IsolatedPosition.prototype['transaction_id'] = undefined;






export default IsolatedPosition;

