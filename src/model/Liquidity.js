/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Liquidity model module.
 * @module model/Liquidity
 * @version 1.0.0
 */
class Liquidity {
    /**
     * Constructs a new <code>Liquidity</code>.
     * @alias module:model/Liquidity
     * @param orderBookId {String} 
     * @param positionId {String} 
     * @param transactionId {String} 
     * @param baseQuantity {String} 
     * @param quoteQuantity {String} 
     * @param sharesQuantity {String} 
     */
    constructor(orderBookId, positionId, transactionId, baseQuantity, quoteQuantity, sharesQuantity) { 
        
        Liquidity.initialize(this, orderBookId, positionId, transactionId, baseQuantity, quoteQuantity, sharesQuantity);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, orderBookId, positionId, transactionId, baseQuantity, quoteQuantity, sharesQuantity) { 
        obj['order_book_id'] = orderBookId;
        obj['position_id'] = positionId;
        obj['transaction_id'] = transactionId;
        obj['base_quantity'] = baseQuantity;
        obj['quote_quantity'] = quoteQuantity;
        obj['shares_quantity'] = sharesQuantity;
    }

    /**
     * Constructs a <code>Liquidity</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Liquidity} obj Optional instance to populate.
     * @return {module:model/Liquidity} The populated <code>Liquidity</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Liquidity();

            if (data.hasOwnProperty('order_book_id')) {
                obj['order_book_id'] = ApiClient.convertToType(data['order_book_id'], 'String');
            }
            if (data.hasOwnProperty('position_id')) {
                obj['position_id'] = ApiClient.convertToType(data['position_id'], 'String');
            }
            if (data.hasOwnProperty('transaction_id')) {
                obj['transaction_id'] = ApiClient.convertToType(data['transaction_id'], 'String');
            }
            if (data.hasOwnProperty('base_quantity')) {
                obj['base_quantity'] = ApiClient.convertToType(data['base_quantity'], 'String');
            }
            if (data.hasOwnProperty('quote_quantity')) {
                obj['quote_quantity'] = ApiClient.convertToType(data['quote_quantity'], 'String');
            }
            if (data.hasOwnProperty('shares_quantity')) {
                obj['shares_quantity'] = ApiClient.convertToType(data['shares_quantity'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Liquidity</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Liquidity</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Liquidity.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['order_book_id'] && !(typeof data['order_book_id'] === 'string' || data['order_book_id'] instanceof String)) {
            throw new Error("Expected the field `order_book_id` to be a primitive type in the JSON string but got " + data['order_book_id']);
        }
        // ensure the json data is a string
        if (data['position_id'] && !(typeof data['position_id'] === 'string' || data['position_id'] instanceof String)) {
            throw new Error("Expected the field `position_id` to be a primitive type in the JSON string but got " + data['position_id']);
        }
        // ensure the json data is a string
        if (data['transaction_id'] && !(typeof data['transaction_id'] === 'string' || data['transaction_id'] instanceof String)) {
            throw new Error("Expected the field `transaction_id` to be a primitive type in the JSON string but got " + data['transaction_id']);
        }
        // ensure the json data is a string
        if (data['base_quantity'] && !(typeof data['base_quantity'] === 'string' || data['base_quantity'] instanceof String)) {
            throw new Error("Expected the field `base_quantity` to be a primitive type in the JSON string but got " + data['base_quantity']);
        }
        // ensure the json data is a string
        if (data['quote_quantity'] && !(typeof data['quote_quantity'] === 'string' || data['quote_quantity'] instanceof String)) {
            throw new Error("Expected the field `quote_quantity` to be a primitive type in the JSON string but got " + data['quote_quantity']);
        }
        // ensure the json data is a string
        if (data['shares_quantity'] && !(typeof data['shares_quantity'] === 'string' || data['shares_quantity'] instanceof String)) {
            throw new Error("Expected the field `shares_quantity` to be a primitive type in the JSON string but got " + data['shares_quantity']);
        }

        return true;
    }


}

Liquidity.RequiredProperties = ["order_book_id", "position_id", "transaction_id", "base_quantity", "quote_quantity", "shares_quantity"];

/**
 * @member {String} order_book_id
 */
Liquidity.prototype['order_book_id'] = undefined;

/**
 * @member {String} position_id
 */
Liquidity.prototype['position_id'] = undefined;

/**
 * @member {String} transaction_id
 */
Liquidity.prototype['transaction_id'] = undefined;

/**
 * @member {String} base_quantity
 */
Liquidity.prototype['base_quantity'] = undefined;

/**
 * @member {String} quote_quantity
 */
Liquidity.prototype['quote_quantity'] = undefined;

/**
 * @member {String} shares_quantity
 */
Liquidity.prototype['shares_quantity'] = undefined;






export default Liquidity;

