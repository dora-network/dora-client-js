/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PriceLevel from './PriceLevel';

/**
 * The LiveOrderbook model module.
 * @module model/LiveOrderbook
 * @version 1.0.0
 */
class LiveOrderbook {
    /**
     * Constructs a new <code>LiveOrderbook</code>.
     * @alias module:model/LiveOrderbook
     * @param bids {Array.<module:model/PriceLevel>} sorted in desc order by price
     * @param asks {Array.<module:model/PriceLevel>} sorted in asc order by price
     */
    constructor(bids, asks) { 
        
        LiveOrderbook.initialize(this, bids, asks);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, bids, asks) { 
        obj['bids'] = bids;
        obj['asks'] = asks;
    }

    /**
     * Constructs a <code>LiveOrderbook</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/LiveOrderbook} obj Optional instance to populate.
     * @return {module:model/LiveOrderbook} The populated <code>LiveOrderbook</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new LiveOrderbook();

            if (data.hasOwnProperty('bids')) {
                obj['bids'] = ApiClient.convertToType(data['bids'], [PriceLevel]);
            }
            if (data.hasOwnProperty('asks')) {
                obj['asks'] = ApiClient.convertToType(data['asks'], [PriceLevel]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>LiveOrderbook</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>LiveOrderbook</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of LiveOrderbook.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['bids']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['bids'])) {
                throw new Error("Expected the field `bids` to be an array in the JSON data but got " + data['bids']);
            }
            // validate the optional field `bids` (array)
            for (const item of data['bids']) {
                PriceLevel.validateJSON(item);
            };
        }
        if (data['asks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['asks'])) {
                throw new Error("Expected the field `asks` to be an array in the JSON data but got " + data['asks']);
            }
            // validate the optional field `asks` (array)
            for (const item of data['asks']) {
                PriceLevel.validateJSON(item);
            };
        }

        return true;
    }


}

LiveOrderbook.RequiredProperties = ["bids", "asks"];

/**
 * sorted in desc order by price
 * @member {Array.<module:model/PriceLevel>} bids
 */
LiveOrderbook.prototype['bids'] = undefined;

/**
 * sorted in asc order by price
 * @member {Array.<module:model/PriceLevel>} asks
 */
LiveOrderbook.prototype['asks'] = undefined;






export default LiveOrderbook;

