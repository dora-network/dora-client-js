/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Margin model module.
 * @module model/Margin
 * @version 1.0.0
 */
class Margin {
    /**
     * Constructs a new <code>Margin</code>.
     * @alias module:model/Margin
     * @param available {String} The total margin available from this position.
     * @param used {String} The amount of margin used from this position.
     * @param remaining {String} The margin remaining available from this position.
     */
    constructor(available, used, remaining) { 
        
        Margin.initialize(this, available, used, remaining);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, available, used, remaining) { 
        obj['available'] = available;
        obj['used'] = used;
        obj['remaining'] = remaining;
    }

    /**
     * Constructs a <code>Margin</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Margin} obj Optional instance to populate.
     * @return {module:model/Margin} The populated <code>Margin</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Margin();

            if (data.hasOwnProperty('available')) {
                obj['available'] = ApiClient.convertToType(data['available'], 'String');
            }
            if (data.hasOwnProperty('used')) {
                obj['used'] = ApiClient.convertToType(data['used'], 'String');
            }
            if (data.hasOwnProperty('remaining')) {
                obj['remaining'] = ApiClient.convertToType(data['remaining'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Margin</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Margin</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Margin.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['available'] && !(typeof data['available'] === 'string' || data['available'] instanceof String)) {
            throw new Error("Expected the field `available` to be a primitive type in the JSON string but got " + data['available']);
        }
        // ensure the json data is a string
        if (data['used'] && !(typeof data['used'] === 'string' || data['used'] instanceof String)) {
            throw new Error("Expected the field `used` to be a primitive type in the JSON string but got " + data['used']);
        }
        // ensure the json data is a string
        if (data['remaining'] && !(typeof data['remaining'] === 'string' || data['remaining'] instanceof String)) {
            throw new Error("Expected the field `remaining` to be a primitive type in the JSON string but got " + data['remaining']);
        }

        return true;
    }


}

Margin.RequiredProperties = ["available", "used", "remaining"];

/**
 * The total margin available from this position.
 * @member {String} available
 */
Margin.prototype['available'] = undefined;

/**
 * The amount of margin used from this position.
 * @member {String} used
 */
Margin.prototype['used'] = undefined;

/**
 * The margin remaining available from this position.
 * @member {String} remaining
 */
Margin.prototype['remaining'] = undefined;






export default Margin;

