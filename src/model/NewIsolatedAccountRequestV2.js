/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The NewIsolatedAccountRequestV2 model module.
 * @module model/NewIsolatedAccountRequestV2
 * @version 1.0.0
 */
class NewIsolatedAccountRequestV2 {
    /**
     * Constructs a new <code>NewIsolatedAccountRequestV2</code>.
     * @alias module:model/NewIsolatedAccountRequestV2
     * @param globalAccountId {String} 
     * @param assetId {String} 
     * @param quantity {Number} 
     */
    constructor(globalAccountId, assetId, quantity) { 
        
        NewIsolatedAccountRequestV2.initialize(this, globalAccountId, assetId, quantity);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, globalAccountId, assetId, quantity) { 
        obj['global_account_id'] = globalAccountId;
        obj['asset_id'] = assetId;
        obj['quantity'] = quantity;
    }

    /**
     * Constructs a <code>NewIsolatedAccountRequestV2</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/NewIsolatedAccountRequestV2} obj Optional instance to populate.
     * @return {module:model/NewIsolatedAccountRequestV2} The populated <code>NewIsolatedAccountRequestV2</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new NewIsolatedAccountRequestV2();

            if (data.hasOwnProperty('global_account_id')) {
                obj['global_account_id'] = ApiClient.convertToType(data['global_account_id'], 'String');
            }
            if (data.hasOwnProperty('asset_id')) {
                obj['asset_id'] = ApiClient.convertToType(data['asset_id'], 'String');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'Number');
            }
            if (data.hasOwnProperty('account_name')) {
                obj['account_name'] = ApiClient.convertToType(data['account_name'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>NewIsolatedAccountRequestV2</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>NewIsolatedAccountRequestV2</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of NewIsolatedAccountRequestV2.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['global_account_id'] && !(typeof data['global_account_id'] === 'string' || data['global_account_id'] instanceof String)) {
            throw new Error("Expected the field `global_account_id` to be a primitive type in the JSON string but got " + data['global_account_id']);
        }
        // ensure the json data is a string
        if (data['asset_id'] && !(typeof data['asset_id'] === 'string' || data['asset_id'] instanceof String)) {
            throw new Error("Expected the field `asset_id` to be a primitive type in the JSON string but got " + data['asset_id']);
        }
        // ensure the json data is a string
        if (data['quantity'] && !(typeof data['quantity'] === 'string' || data['quantity'] instanceof String)) {
            throw new Error("Expected the field `quantity` to be a primitive type in the JSON string but got " + data['quantity']);
        }
        // ensure the json data is a string
        if (data['account_name'] && !(typeof data['account_name'] === 'string' || data['account_name'] instanceof String)) {
            throw new Error("Expected the field `account_name` to be a primitive type in the JSON string but got " + data['account_name']);
        }

        return true;
    }


}

NewIsolatedAccountRequestV2.RequiredProperties = ["global_account_id", "asset_id", "quantity"];

/**
 * @member {String} global_account_id
 */
NewIsolatedAccountRequestV2.prototype['global_account_id'] = undefined;

/**
 * @member {String} asset_id
 */
NewIsolatedAccountRequestV2.prototype['asset_id'] = undefined;

/**
 * @member {Number} quantity
 */
NewIsolatedAccountRequestV2.prototype['quantity'] = undefined;

/**
 * @member {String} account_name
 */
NewIsolatedAccountRequestV2.prototype['account_name'] = undefined;






export default NewIsolatedAccountRequestV2;

