/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The OrderBookBalance model module.
 * @module model/OrderBookBalance
 * @version 1.0.0
 */
class OrderBookBalance {
    /**
     * Constructs a new <code>OrderBookBalance</code>.
     * @alias module:model/OrderBookBalance
     * @param orderBookId {String} The ID of the order book.
     * @param baseQuantity {Number} The quantity of the base asset.
     * @param quoteQuantity {Number} The quantity of the quote asset.
     * @param sharesQuantity {Number} The quantity of pool shares.
     */
    constructor(orderBookId, baseQuantity, quoteQuantity, sharesQuantity) { 
        
        OrderBookBalance.initialize(this, orderBookId, baseQuantity, quoteQuantity, sharesQuantity);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, orderBookId, baseQuantity, quoteQuantity, sharesQuantity) { 
        obj['order_book_id'] = orderBookId;
        obj['base_quantity'] = baseQuantity;
        obj['quote_quantity'] = quoteQuantity;
        obj['shares_quantity'] = sharesQuantity;
    }

    /**
     * Constructs a <code>OrderBookBalance</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrderBookBalance} obj Optional instance to populate.
     * @return {module:model/OrderBookBalance} The populated <code>OrderBookBalance</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrderBookBalance();

            if (data.hasOwnProperty('order_book_id')) {
                obj['order_book_id'] = ApiClient.convertToType(data['order_book_id'], 'String');
            }
            if (data.hasOwnProperty('base_quantity')) {
                obj['base_quantity'] = ApiClient.convertToType(data['base_quantity'], 'Number');
            }
            if (data.hasOwnProperty('quote_quantity')) {
                obj['quote_quantity'] = ApiClient.convertToType(data['quote_quantity'], 'Number');
            }
            if (data.hasOwnProperty('shares_quantity')) {
                obj['shares_quantity'] = ApiClient.convertToType(data['shares_quantity'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrderBookBalance</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrderBookBalance</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OrderBookBalance.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['order_book_id'] && !(typeof data['order_book_id'] === 'string' || data['order_book_id'] instanceof String)) {
            throw new Error("Expected the field `order_book_id` to be a primitive type in the JSON string but got " + data['order_book_id']);
        }

        return true;
    }


}

OrderBookBalance.RequiredProperties = ["order_book_id", "base_quantity", "quote_quantity", "shares_quantity"];

/**
 * The ID of the order book.
 * @member {String} order_book_id
 */
OrderBookBalance.prototype['order_book_id'] = undefined;

/**
 * The quantity of the base asset.
 * @member {Number} base_quantity
 */
OrderBookBalance.prototype['base_quantity'] = undefined;

/**
 * The quantity of the quote asset.
 * @member {Number} quote_quantity
 */
OrderBookBalance.prototype['quote_quantity'] = undefined;

/**
 * The quantity of pool shares.
 * @member {Number} shares_quantity
 */
OrderBookBalance.prototype['shares_quantity'] = undefined;






export default OrderBookBalance;

