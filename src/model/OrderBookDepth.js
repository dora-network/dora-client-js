/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PriceLevel from './PriceLevel';

/**
 * The OrderBookDepth model module.
 * @module model/OrderBookDepth
 * @version 1.0.0
 */
class OrderBookDepth {
    /**
     * Constructs a new <code>OrderBookDepth</code>.
     * @alias module:model/OrderBookDepth
     * @param orderBookId {String} 
     * @param bids {Array.<module:model/PriceLevel>} 
     * @param asks {Array.<module:model/PriceLevel>} 
     * @param timestamp {Date} 
     */
    constructor(orderBookId, bids, asks, timestamp) { 
        
        OrderBookDepth.initialize(this, orderBookId, bids, asks, timestamp);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, orderBookId, bids, asks, timestamp) { 
        obj['order_book_id'] = orderBookId;
        obj['bids'] = bids;
        obj['asks'] = asks;
        obj['timestamp'] = timestamp;
    }

    /**
     * Constructs a <code>OrderBookDepth</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrderBookDepth} obj Optional instance to populate.
     * @return {module:model/OrderBookDepth} The populated <code>OrderBookDepth</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrderBookDepth();

            if (data.hasOwnProperty('order_book_id')) {
                obj['order_book_id'] = ApiClient.convertToType(data['order_book_id'], 'String');
            }
            if (data.hasOwnProperty('bids')) {
                obj['bids'] = ApiClient.convertToType(data['bids'], [PriceLevel]);
            }
            if (data.hasOwnProperty('asks')) {
                obj['asks'] = ApiClient.convertToType(data['asks'], [PriceLevel]);
            }
            if (data.hasOwnProperty('timestamp')) {
                obj['timestamp'] = ApiClient.convertToType(data['timestamp'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrderBookDepth</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrderBookDepth</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OrderBookDepth.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['order_book_id'] && !(typeof data['order_book_id'] === 'string' || data['order_book_id'] instanceof String)) {
            throw new Error("Expected the field `order_book_id` to be a primitive type in the JSON string but got " + data['order_book_id']);
        }
        if (data['bids']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['bids'])) {
                throw new Error("Expected the field `bids` to be an array in the JSON data but got " + data['bids']);
            }
            // validate the optional field `bids` (array)
            for (const item of data['bids']) {
                PriceLevel.validateJSON(item);
            };
        }
        if (data['asks']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['asks'])) {
                throw new Error("Expected the field `asks` to be an array in the JSON data but got " + data['asks']);
            }
            // validate the optional field `asks` (array)
            for (const item of data['asks']) {
                PriceLevel.validateJSON(item);
            };
        }

        return true;
    }


}

OrderBookDepth.RequiredProperties = ["order_book_id", "bids", "asks", "timestamp"];

/**
 * @member {String} order_book_id
 */
OrderBookDepth.prototype['order_book_id'] = undefined;

/**
 * @member {Array.<module:model/PriceLevel>} bids
 */
OrderBookDepth.prototype['bids'] = undefined;

/**
 * @member {Array.<module:model/PriceLevel>} asks
 */
OrderBookDepth.prototype['asks'] = undefined;

/**
 * @member {Date} timestamp
 */
OrderBookDepth.prototype['timestamp'] = undefined;






export default OrderBookDepth;

