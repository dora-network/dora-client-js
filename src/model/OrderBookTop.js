/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The OrderBookTop model module.
 * @module model/OrderBookTop
 * @version 1.0.0
 */
class OrderBookTop {
    /**
     * Constructs a new <code>OrderBookTop</code>.
     * @alias module:model/OrderBookTop
     * @param orderBookId {String} 
     * @param bestBid {String} 
     * @param bestAsk {String} 
     * @param timestamp {Date} 
     */
    constructor(orderBookId, bestBid, bestAsk, timestamp) { 
        
        OrderBookTop.initialize(this, orderBookId, bestBid, bestAsk, timestamp);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, orderBookId, bestBid, bestAsk, timestamp) { 
        obj['order_book_id'] = orderBookId;
        obj['best_bid'] = bestBid;
        obj['best_ask'] = bestAsk;
        obj['timestamp'] = timestamp;
    }

    /**
     * Constructs a <code>OrderBookTop</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/OrderBookTop} obj Optional instance to populate.
     * @return {module:model/OrderBookTop} The populated <code>OrderBookTop</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OrderBookTop();

            if (data.hasOwnProperty('order_book_id')) {
                obj['order_book_id'] = ApiClient.convertToType(data['order_book_id'], 'String');
            }
            if (data.hasOwnProperty('best_bid')) {
                obj['best_bid'] = ApiClient.convertToType(data['best_bid'], 'String');
            }
            if (data.hasOwnProperty('best_ask')) {
                obj['best_ask'] = ApiClient.convertToType(data['best_ask'], 'String');
            }
            if (data.hasOwnProperty('timestamp')) {
                obj['timestamp'] = ApiClient.convertToType(data['timestamp'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OrderBookTop</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OrderBookTop</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of OrderBookTop.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['order_book_id'] && !(typeof data['order_book_id'] === 'string' || data['order_book_id'] instanceof String)) {
            throw new Error("Expected the field `order_book_id` to be a primitive type in the JSON string but got " + data['order_book_id']);
        }
        // ensure the json data is a string
        if (data['best_bid'] && !(typeof data['best_bid'] === 'string' || data['best_bid'] instanceof String)) {
            throw new Error("Expected the field `best_bid` to be a primitive type in the JSON string but got " + data['best_bid']);
        }
        // ensure the json data is a string
        if (data['best_ask'] && !(typeof data['best_ask'] === 'string' || data['best_ask'] instanceof String)) {
            throw new Error("Expected the field `best_ask` to be a primitive type in the JSON string but got " + data['best_ask']);
        }

        return true;
    }


}

OrderBookTop.RequiredProperties = ["order_book_id", "best_bid", "best_ask", "timestamp"];

/**
 * @member {String} order_book_id
 */
OrderBookTop.prototype['order_book_id'] = undefined;

/**
 * @member {String} best_bid
 */
OrderBookTop.prototype['best_bid'] = undefined;

/**
 * @member {String} best_ask
 */
OrderBookTop.prototype['best_ask'] = undefined;

/**
 * @member {Date} timestamp
 */
OrderBookTop.prototype['timestamp'] = undefined;






export default OrderBookTop;

