/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PLAsset from './PLAsset';
import PLSummary from './PLSummary';

/**
 * The PLAccount model module.
 * @module model/PLAccount
 * @version 1.0.0
 */
class PLAccount {
    /**
     * Constructs a new <code>PLAccount</code>.
     * @alias module:model/PLAccount
     * @param accountId {String} The ID of the account holding the position
     * @param accountName {String} The name of the account holding the position
     * @param isGlobal {Boolean} Whether the account is the global or an isolated account
     * @param assets {Array.<module:model/PLAsset>} 
     * @param summary {module:model/PLSummary} 
     */
    constructor(accountId, accountName, isGlobal, assets, summary) { 
        
        PLAccount.initialize(this, accountId, accountName, isGlobal, assets, summary);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, accountId, accountName, isGlobal, assets, summary) { 
        obj['account_id'] = accountId;
        obj['account_name'] = accountName;
        obj['is_global'] = isGlobal;
        obj['assets'] = assets;
        obj['summary'] = summary;
    }

    /**
     * Constructs a <code>PLAccount</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PLAccount} obj Optional instance to populate.
     * @return {module:model/PLAccount} The populated <code>PLAccount</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PLAccount();

            if (data.hasOwnProperty('account_id')) {
                obj['account_id'] = ApiClient.convertToType(data['account_id'], 'String');
            }
            if (data.hasOwnProperty('account_name')) {
                obj['account_name'] = ApiClient.convertToType(data['account_name'], 'String');
            }
            if (data.hasOwnProperty('is_global')) {
                obj['is_global'] = ApiClient.convertToType(data['is_global'], 'Boolean');
            }
            if (data.hasOwnProperty('assets')) {
                obj['assets'] = ApiClient.convertToType(data['assets'], [PLAsset]);
            }
            if (data.hasOwnProperty('summary')) {
                obj['summary'] = PLSummary.constructFromObject(data['summary']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PLAccount</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PLAccount</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PLAccount.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['account_id'] && !(typeof data['account_id'] === 'string' || data['account_id'] instanceof String)) {
            throw new Error("Expected the field `account_id` to be a primitive type in the JSON string but got " + data['account_id']);
        }
        // ensure the json data is a string
        if (data['account_name'] && !(typeof data['account_name'] === 'string' || data['account_name'] instanceof String)) {
            throw new Error("Expected the field `account_name` to be a primitive type in the JSON string but got " + data['account_name']);
        }
        if (data['assets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['assets'])) {
                throw new Error("Expected the field `assets` to be an array in the JSON data but got " + data['assets']);
            }
            // validate the optional field `assets` (array)
            for (const item of data['assets']) {
                PLAsset.validateJSON(item);
            };
        }
        // validate the optional field `summary`
        if (data['summary']) { // data not null
          PLSummary.validateJSON(data['summary']);
        }

        return true;
    }


}

PLAccount.RequiredProperties = ["account_id", "account_name", "is_global", "assets", "summary"];

/**
 * The ID of the account holding the position
 * @member {String} account_id
 */
PLAccount.prototype['account_id'] = undefined;

/**
 * The name of the account holding the position
 * @member {String} account_name
 */
PLAccount.prototype['account_name'] = undefined;

/**
 * Whether the account is the global or an isolated account
 * @member {Boolean} is_global
 */
PLAccount.prototype['is_global'] = undefined;

/**
 * @member {Array.<module:model/PLAsset>} assets
 */
PLAccount.prototype['assets'] = undefined;

/**
 * @member {module:model/PLSummary} summary
 */
PLAccount.prototype['summary'] = undefined;






export default PLAccount;

