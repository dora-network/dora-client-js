/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PLSummary model module.
 * @module model/PLSummary
 * @version 1.0.0
 */
class PLSummary {
    /**
     * Constructs a new <code>PLSummary</code>.
     * @alias module:model/PLSummary
     * @param leverage {String} The leverage used to obtain the position on the isolated account
     * @param accountEquity {String} 
     * @param available {String} 
     * @param health {String} 
     * @param ltv {String} 
     */
    constructor(leverage, accountEquity, available, health, ltv) { 
        
        PLSummary.initialize(this, leverage, accountEquity, available, health, ltv);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, leverage, accountEquity, available, health, ltv) { 
        obj['leverage'] = leverage;
        obj['account_equity'] = accountEquity;
        obj['available'] = available;
        obj['health'] = health;
        obj['ltv'] = ltv;
    }

    /**
     * Constructs a <code>PLSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PLSummary} obj Optional instance to populate.
     * @return {module:model/PLSummary} The populated <code>PLSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PLSummary();

            if (data.hasOwnProperty('leverage')) {
                obj['leverage'] = ApiClient.convertToType(data['leverage'], 'String');
            }
            if (data.hasOwnProperty('account_equity')) {
                obj['account_equity'] = ApiClient.convertToType(data['account_equity'], 'String');
            }
            if (data.hasOwnProperty('available')) {
                obj['available'] = ApiClient.convertToType(data['available'], 'String');
            }
            if (data.hasOwnProperty('health')) {
                obj['health'] = ApiClient.convertToType(data['health'], 'String');
            }
            if (data.hasOwnProperty('ltv')) {
                obj['ltv'] = ApiClient.convertToType(data['ltv'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PLSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PLSummary</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PLSummary.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['leverage'] && !(typeof data['leverage'] === 'string' || data['leverage'] instanceof String)) {
            throw new Error("Expected the field `leverage` to be a primitive type in the JSON string but got " + data['leverage']);
        }
        // ensure the json data is a string
        if (data['account_equity'] && !(typeof data['account_equity'] === 'string' || data['account_equity'] instanceof String)) {
            throw new Error("Expected the field `account_equity` to be a primitive type in the JSON string but got " + data['account_equity']);
        }
        // ensure the json data is a string
        if (data['available'] && !(typeof data['available'] === 'string' || data['available'] instanceof String)) {
            throw new Error("Expected the field `available` to be a primitive type in the JSON string but got " + data['available']);
        }
        // ensure the json data is a string
        if (data['health'] && !(typeof data['health'] === 'string' || data['health'] instanceof String)) {
            throw new Error("Expected the field `health` to be a primitive type in the JSON string but got " + data['health']);
        }
        // ensure the json data is a string
        if (data['ltv'] && !(typeof data['ltv'] === 'string' || data['ltv'] instanceof String)) {
            throw new Error("Expected the field `ltv` to be a primitive type in the JSON string but got " + data['ltv']);
        }

        return true;
    }


}

PLSummary.RequiredProperties = ["leverage", "account_equity", "available", "health", "ltv"];

/**
 * The leverage used to obtain the position on the isolated account
 * @member {String} leverage
 */
PLSummary.prototype['leverage'] = undefined;

/**
 * @member {String} account_equity
 */
PLSummary.prototype['account_equity'] = undefined;

/**
 * @member {String} available
 */
PLSummary.prototype['available'] = undefined;

/**
 * @member {String} health
 */
PLSummary.prototype['health'] = undefined;

/**
 * @member {String} ltv
 */
PLSummary.prototype['ltv'] = undefined;






export default PLSummary;

