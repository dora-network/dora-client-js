/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PermitDomain model module.
 * @module model/PermitDomain
 * @version 1.0.0
 */
class PermitDomain {
    /**
     * Constructs a new <code>PermitDomain</code>.
     * The EIP-712 domain separator data for the USDC token on this chain.
     * @alias module:model/PermitDomain
     * @param name {String} EIP-712 domain name of the USDC token, e.g. 'USD Coin'.
     * @param version {String} EIP-712 domain version of the USDC token, e.g. '2'.
     * @param chainId {Number} EVM chain ID, as a JSON number.
     * @param verifyingContract {String} The USDC token contract address, as a 0x-prefixed hex string.
     */
    constructor(name, version, chainId, verifyingContract) { 
        
        PermitDomain.initialize(this, name, version, chainId, verifyingContract);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, version, chainId, verifyingContract) { 
        obj['name'] = name;
        obj['version'] = version;
        obj['chainId'] = chainId;
        obj['verifyingContract'] = verifyingContract;
    }

    /**
     * Constructs a <code>PermitDomain</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PermitDomain} obj Optional instance to populate.
     * @return {module:model/PermitDomain} The populated <code>PermitDomain</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PermitDomain();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('version')) {
                obj['version'] = ApiClient.convertToType(data['version'], 'String');
            }
            if (data.hasOwnProperty('chainId')) {
                obj['chainId'] = ApiClient.convertToType(data['chainId'], 'Number');
            }
            if (data.hasOwnProperty('verifyingContract')) {
                obj['verifyingContract'] = ApiClient.convertToType(data['verifyingContract'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PermitDomain</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PermitDomain</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PermitDomain.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['version'] && !(typeof data['version'] === 'string' || data['version'] instanceof String)) {
            throw new Error("Expected the field `version` to be a primitive type in the JSON string but got " + data['version']);
        }
        // ensure the json data is a string
        if (data['verifyingContract'] && !(typeof data['verifyingContract'] === 'string' || data['verifyingContract'] instanceof String)) {
            throw new Error("Expected the field `verifyingContract` to be a primitive type in the JSON string but got " + data['verifyingContract']);
        }

        return true;
    }


}

PermitDomain.RequiredProperties = ["name", "version", "chainId", "verifyingContract"];

/**
 * EIP-712 domain name of the USDC token, e.g. 'USD Coin'.
 * @member {String} name
 */
PermitDomain.prototype['name'] = undefined;

/**
 * EIP-712 domain version of the USDC token, e.g. '2'.
 * @member {String} version
 */
PermitDomain.prototype['version'] = undefined;

/**
 * EVM chain ID, as a JSON number.
 * @member {Number} chainId
 */
PermitDomain.prototype['chainId'] = undefined;

/**
 * The USDC token contract address, as a 0x-prefixed hex string.
 * @member {String} verifyingContract
 */
PermitDomain.prototype['verifyingContract'] = undefined;






export default PermitDomain;

