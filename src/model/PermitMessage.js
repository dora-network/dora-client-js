/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PermitMessage model module.
 * @module model/PermitMessage
 * @version 1.0.0
 */
class PermitMessage {
    /**
     * Constructs a new <code>PermitMessage</code>.
     * The EIP-2612 Permit struct the user signs. The uint256 fields are decimal strings to avoid JSON number precision loss.
     * @alias module:model/PermitMessage
     * @param owner {String} The user's wallet address (permit owner), as a 0x-prefixed hex string.
     * @param spender {String} The vault contract address granted the allowance, as a 0x-prefixed hex string.
     * @param value {String} Approved quantity in USDC base units (10^-6 USDC), as a decimal string.
     * @param nonce {String} The owner's current USDC permit nonce on this chain, as a decimal string.
     * @param deadline {String} Unix timestamp (seconds) at which the permit expires, as a decimal string. One hour from issuance.
     */
    constructor(owner, spender, value, nonce, deadline) { 
        
        PermitMessage.initialize(this, owner, spender, value, nonce, deadline);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, owner, spender, value, nonce, deadline) { 
        obj['owner'] = owner;
        obj['spender'] = spender;
        obj['value'] = value;
        obj['nonce'] = nonce;
        obj['deadline'] = deadline;
    }

    /**
     * Constructs a <code>PermitMessage</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PermitMessage} obj Optional instance to populate.
     * @return {module:model/PermitMessage} The populated <code>PermitMessage</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PermitMessage();

            if (data.hasOwnProperty('owner')) {
                obj['owner'] = ApiClient.convertToType(data['owner'], 'String');
            }
            if (data.hasOwnProperty('spender')) {
                obj['spender'] = ApiClient.convertToType(data['spender'], 'String');
            }
            if (data.hasOwnProperty('value')) {
                obj['value'] = ApiClient.convertToType(data['value'], 'String');
            }
            if (data.hasOwnProperty('nonce')) {
                obj['nonce'] = ApiClient.convertToType(data['nonce'], 'String');
            }
            if (data.hasOwnProperty('deadline')) {
                obj['deadline'] = ApiClient.convertToType(data['deadline'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PermitMessage</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PermitMessage</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PermitMessage.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['owner'] && !(typeof data['owner'] === 'string' || data['owner'] instanceof String)) {
            throw new Error("Expected the field `owner` to be a primitive type in the JSON string but got " + data['owner']);
        }
        // ensure the json data is a string
        if (data['spender'] && !(typeof data['spender'] === 'string' || data['spender'] instanceof String)) {
            throw new Error("Expected the field `spender` to be a primitive type in the JSON string but got " + data['spender']);
        }
        // ensure the json data is a string
        if (data['value'] && !(typeof data['value'] === 'string' || data['value'] instanceof String)) {
            throw new Error("Expected the field `value` to be a primitive type in the JSON string but got " + data['value']);
        }
        // ensure the json data is a string
        if (data['nonce'] && !(typeof data['nonce'] === 'string' || data['nonce'] instanceof String)) {
            throw new Error("Expected the field `nonce` to be a primitive type in the JSON string but got " + data['nonce']);
        }
        // ensure the json data is a string
        if (data['deadline'] && !(typeof data['deadline'] === 'string' || data['deadline'] instanceof String)) {
            throw new Error("Expected the field `deadline` to be a primitive type in the JSON string but got " + data['deadline']);
        }

        return true;
    }


}

PermitMessage.RequiredProperties = ["owner", "spender", "value", "nonce", "deadline"];

/**
 * The user's wallet address (permit owner), as a 0x-prefixed hex string.
 * @member {String} owner
 */
PermitMessage.prototype['owner'] = undefined;

/**
 * The vault contract address granted the allowance, as a 0x-prefixed hex string.
 * @member {String} spender
 */
PermitMessage.prototype['spender'] = undefined;

/**
 * Approved quantity in USDC base units (10^-6 USDC), as a decimal string.
 * @member {String} value
 */
PermitMessage.prototype['value'] = undefined;

/**
 * The owner's current USDC permit nonce on this chain, as a decimal string.
 * @member {String} nonce
 */
PermitMessage.prototype['nonce'] = undefined;

/**
 * Unix timestamp (seconds) at which the permit expires, as a decimal string. One hour from issuance.
 * @member {String} deadline
 */
PermitMessage.prototype['deadline'] = undefined;






export default PermitMessage;

