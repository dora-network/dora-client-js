/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PermitDomain from './PermitDomain';
import PermitMessage from './PermitMessage';
import TypedDataField from './TypedDataField';

/**
 * The PermitTypedData model module.
 * @module model/PermitTypedData
 * @version 1.0.0
 */
class PermitTypedData {
    /**
     * Constructs a new <code>PermitTypedData</code>.
     * An EIP-712 typed-data message in the exact shape expected by eth_signTypedData_v4. The user signs it to authorize the vault as a USDC spender via EIP-2612 permit.
     * @alias module:model/PermitTypedData
     * @param types {Object.<String, Array.<module:model/TypedDataField>>} EIP-712 type definitions, keyed by type name ('EIP712Domain' and 'Permit').
     * @param primaryType {String} Always 'Permit'.
     * @param domain {module:model/PermitDomain} 
     * @param message {module:model/PermitMessage} 
     */
    constructor(types, primaryType, domain, message) { 
        
        PermitTypedData.initialize(this, types, primaryType, domain, message);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, types, primaryType, domain, message) { 
        obj['types'] = types;
        obj['primaryType'] = primaryType;
        obj['domain'] = domain;
        obj['message'] = message;
    }

    /**
     * Constructs a <code>PermitTypedData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PermitTypedData} obj Optional instance to populate.
     * @return {module:model/PermitTypedData} The populated <code>PermitTypedData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PermitTypedData();

            if (data.hasOwnProperty('types')) {
                obj['types'] = ApiClient.convertToType(data['types'], {'String': [TypedDataField]});
            }
            if (data.hasOwnProperty('primaryType')) {
                obj['primaryType'] = ApiClient.convertToType(data['primaryType'], 'String');
            }
            if (data.hasOwnProperty('domain')) {
                obj['domain'] = PermitDomain.constructFromObject(data['domain']);
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = PermitMessage.constructFromObject(data['message']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PermitTypedData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PermitTypedData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PermitTypedData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['primaryType'] && !(typeof data['primaryType'] === 'string' || data['primaryType'] instanceof String)) {
            throw new Error("Expected the field `primaryType` to be a primitive type in the JSON string but got " + data['primaryType']);
        }
        // validate the optional field `domain`
        if (data['domain']) { // data not null
          PermitDomain.validateJSON(data['domain']);
        }
        // validate the optional field `message`
        if (data['message']) { // data not null
          PermitMessage.validateJSON(data['message']);
        }

        return true;
    }


}

PermitTypedData.RequiredProperties = ["types", "primaryType", "domain", "message"];

/**
 * EIP-712 type definitions, keyed by type name ('EIP712Domain' and 'Permit').
 * @member {Object.<String, Array.<module:model/TypedDataField>>} types
 */
PermitTypedData.prototype['types'] = undefined;

/**
 * Always 'Permit'.
 * @member {String} primaryType
 */
PermitTypedData.prototype['primaryType'] = undefined;

/**
 * @member {module:model/PermitDomain} domain
 */
PermitTypedData.prototype['domain'] = undefined;

/**
 * @member {module:model/PermitMessage} message
 */
PermitTypedData.prototype['message'] = undefined;






export default PermitTypedData;

