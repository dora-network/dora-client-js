/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PnLRankingResponse from './PnLRankingResponse';
import PnLRankingSummary from './PnLRankingSummary';

/**
 * The PnLRankingResponses model module.
 * @module model/PnLRankingResponses
 * @version 1.0.0
 */
class PnLRankingResponses {
    /**
     * Constructs a new <code>PnLRankingResponses</code>.
     * @alias module:model/PnLRankingResponses
     * @param summary {module:model/PnLRankingSummary} 
     * @param rankings {Array.<module:model/PnLRankingResponse>} 
     */
    constructor(summary, rankings) { 
        
        PnLRankingResponses.initialize(this, summary, rankings);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, summary, rankings) { 
        obj['summary'] = summary;
        obj['rankings'] = rankings;
    }

    /**
     * Constructs a <code>PnLRankingResponses</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PnLRankingResponses} obj Optional instance to populate.
     * @return {module:model/PnLRankingResponses} The populated <code>PnLRankingResponses</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PnLRankingResponses();

            if (data.hasOwnProperty('summary')) {
                obj['summary'] = PnLRankingSummary.constructFromObject(data['summary']);
            }
            if (data.hasOwnProperty('rankings')) {
                obj['rankings'] = ApiClient.convertToType(data['rankings'], [PnLRankingResponse]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PnLRankingResponses</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PnLRankingResponses</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PnLRankingResponses.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // validate the optional field `summary`
        if (data['summary']) { // data not null
          PnLRankingSummary.validateJSON(data['summary']);
        }
        if (data['rankings']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['rankings'])) {
                throw new Error("Expected the field `rankings` to be an array in the JSON data but got " + data['rankings']);
            }
            // validate the optional field `rankings` (array)
            for (const item of data['rankings']) {
                PnLRankingResponse.validateJSON(item);
            };
        }

        return true;
    }


}

PnLRankingResponses.RequiredProperties = ["summary", "rankings"];

/**
 * @member {module:model/PnLRankingSummary} summary
 */
PnLRankingResponses.prototype['summary'] = undefined;

/**
 * @member {Array.<module:model/PnLRankingResponse>} rankings
 */
PnLRankingResponses.prototype['rankings'] = undefined;






export default PnLRankingResponses;

