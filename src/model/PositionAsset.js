/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AssetKind from './AssetKind';

/**
 * The PositionAsset model module.
 * @module model/PositionAsset
 * @version 1.0.0
 */
class PositionAsset {
    /**
     * Constructs a new <code>PositionAsset</code>.
     * @alias module:model/PositionAsset
     * @param assetId {String} 
     * @param kind {module:model/AssetKind} 
     * @param borrowed {String} 
     * @param impendingBorrows {String} 
     * @param available {String} 
     * @param locked {String} 
     */
    constructor(assetId, kind, borrowed, impendingBorrows, available, locked) { 
        
        PositionAsset.initialize(this, assetId, kind, borrowed, impendingBorrows, available, locked);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, assetId, kind, borrowed, impendingBorrows, available, locked) { 
        obj['asset_id'] = assetId;
        obj['kind'] = kind;
        obj['borrowed'] = borrowed;
        obj['impending_borrows'] = impendingBorrows;
        obj['available'] = available;
        obj['locked'] = locked;
    }

    /**
     * Constructs a <code>PositionAsset</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PositionAsset} obj Optional instance to populate.
     * @return {module:model/PositionAsset} The populated <code>PositionAsset</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PositionAsset();

            if (data.hasOwnProperty('asset_id')) {
                obj['asset_id'] = ApiClient.convertToType(data['asset_id'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = AssetKind.constructFromObject(data['kind']);
            }
            if (data.hasOwnProperty('borrowed')) {
                obj['borrowed'] = ApiClient.convertToType(data['borrowed'], 'String');
            }
            if (data.hasOwnProperty('impending_borrows')) {
                obj['impending_borrows'] = ApiClient.convertToType(data['impending_borrows'], 'String');
            }
            if (data.hasOwnProperty('available')) {
                obj['available'] = ApiClient.convertToType(data['available'], 'String');
            }
            if (data.hasOwnProperty('locked')) {
                obj['locked'] = ApiClient.convertToType(data['locked'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PositionAsset</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PositionAsset</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PositionAsset.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['asset_id'] && !(typeof data['asset_id'] === 'string' || data['asset_id'] instanceof String)) {
            throw new Error("Expected the field `asset_id` to be a primitive type in the JSON string but got " + data['asset_id']);
        }
        // ensure the json data is a string
        if (data['borrowed'] && !(typeof data['borrowed'] === 'string' || data['borrowed'] instanceof String)) {
            throw new Error("Expected the field `borrowed` to be a primitive type in the JSON string but got " + data['borrowed']);
        }
        // ensure the json data is a string
        if (data['impending_borrows'] && !(typeof data['impending_borrows'] === 'string' || data['impending_borrows'] instanceof String)) {
            throw new Error("Expected the field `impending_borrows` to be a primitive type in the JSON string but got " + data['impending_borrows']);
        }
        // ensure the json data is a string
        if (data['available'] && !(typeof data['available'] === 'string' || data['available'] instanceof String)) {
            throw new Error("Expected the field `available` to be a primitive type in the JSON string but got " + data['available']);
        }
        // ensure the json data is a string
        if (data['locked'] && !(typeof data['locked'] === 'string' || data['locked'] instanceof String)) {
            throw new Error("Expected the field `locked` to be a primitive type in the JSON string but got " + data['locked']);
        }

        return true;
    }


}

PositionAsset.RequiredProperties = ["asset_id", "kind", "borrowed", "impending_borrows", "available", "locked"];

/**
 * @member {String} asset_id
 */
PositionAsset.prototype['asset_id'] = undefined;

/**
 * @member {module:model/AssetKind} kind
 */
PositionAsset.prototype['kind'] = undefined;

/**
 * @member {String} borrowed
 */
PositionAsset.prototype['borrowed'] = undefined;

/**
 * @member {String} impending_borrows
 */
PositionAsset.prototype['impending_borrows'] = undefined;

/**
 * @member {String} available
 */
PositionAsset.prototype['available'] = undefined;

/**
 * @member {String} locked
 */
PositionAsset.prototype['locked'] = undefined;






export default PositionAsset;

