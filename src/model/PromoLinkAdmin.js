/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PromoLinkStatus from './PromoLinkStatus';

/**
 * The PromoLinkAdmin model module.
 * @module model/PromoLinkAdmin
 * @version 1.0.0
 */
class PromoLinkAdmin {
    /**
     * Constructs a new <code>PromoLinkAdmin</code>.
     * @alias module:model/PromoLinkAdmin
     * @param id {String} 
     * @param tokenPrefix {String} 
     * @param status {module:model/PromoLinkStatus} 
     * @param expiresAt {Date} 
     */
    constructor(id, tokenPrefix, status, expiresAt) { 
        
        PromoLinkAdmin.initialize(this, id, tokenPrefix, status, expiresAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, tokenPrefix, status, expiresAt) { 
        obj['id'] = id;
        obj['token_prefix'] = tokenPrefix;
        obj['status'] = status;
        obj['expires_at'] = expiresAt;
    }

    /**
     * Constructs a <code>PromoLinkAdmin</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PromoLinkAdmin} obj Optional instance to populate.
     * @return {module:model/PromoLinkAdmin} The populated <code>PromoLinkAdmin</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PromoLinkAdmin();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('token_prefix')) {
                obj['token_prefix'] = ApiClient.convertToType(data['token_prefix'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = PromoLinkStatus.constructFromObject(data['status']);
            }
            if (data.hasOwnProperty('expires_at')) {
                obj['expires_at'] = ApiClient.convertToType(data['expires_at'], 'Date');
            }
            if (data.hasOwnProperty('claimed_at')) {
                obj['claimed_at'] = ApiClient.convertToType(data['claimed_at'], 'Date');
            }
            if (data.hasOwnProperty('claimed_email')) {
                obj['claimed_email'] = ApiClient.convertToType(data['claimed_email'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PromoLinkAdmin</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PromoLinkAdmin</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PromoLinkAdmin.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['token_prefix'] && !(typeof data['token_prefix'] === 'string' || data['token_prefix'] instanceof String)) {
            throw new Error("Expected the field `token_prefix` to be a primitive type in the JSON string but got " + data['token_prefix']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }
        // ensure the json data is a string
        if (data['claimed_email'] && !(typeof data['claimed_email'] === 'string' || data['claimed_email'] instanceof String)) {
            throw new Error("Expected the field `claimed_email` to be a primitive type in the JSON string but got " + data['claimed_email']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }

        return true;
    }


}

PromoLinkAdmin.RequiredProperties = ["id", "token_prefix", "status", "expires_at"];

/**
 * @member {String} id
 */
PromoLinkAdmin.prototype['id'] = undefined;

/**
 * @member {String} token_prefix
 */
PromoLinkAdmin.prototype['token_prefix'] = undefined;

/**
 * Only present when reveal=true. Private; do not log or cache.
 * @member {String} url
 */
PromoLinkAdmin.prototype['url'] = undefined;

/**
 * @member {module:model/PromoLinkStatus} status
 */
PromoLinkAdmin.prototype['status'] = undefined;

/**
 * @member {Date} expires_at
 */
PromoLinkAdmin.prototype['expires_at'] = undefined;

/**
 * @member {Date} claimed_at
 */
PromoLinkAdmin.prototype['claimed_at'] = undefined;

/**
 * Masked as the first character, three asterisks, and domain.
 * @member {String} claimed_email
 */
PromoLinkAdmin.prototype['claimed_email'] = undefined;

/**
 * @member {String} user_id
 */
PromoLinkAdmin.prototype['user_id'] = undefined;






export default PromoLinkAdmin;

