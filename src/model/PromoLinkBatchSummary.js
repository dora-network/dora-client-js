/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import PromoSourceType from './PromoSourceType';

/**
 * The PromoLinkBatchSummary model module.
 * @module model/PromoLinkBatchSummary
 * @version 1.0.0
 */
class PromoLinkBatchSummary {
    /**
     * Constructs a new <code>PromoLinkBatchSummary</code>.
     * @alias module:model/PromoLinkBatchSummary
     * @param id {String} 
     * @param sourceType {module:model/PromoSourceType} 
     * @param sourceId {String} 
     * @param sourceName {String} 
     * @param allocation {Number} 
     * @param issued {Number} 
     * @param claimed {Number} 
     * @param revoked {Number} 
     * @param createdAt {Date} 
     */
    constructor(id, sourceType, sourceId, sourceName, allocation, issued, claimed, revoked, createdAt) { 
        
        PromoLinkBatchSummary.initialize(this, id, sourceType, sourceId, sourceName, allocation, issued, claimed, revoked, createdAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, sourceType, sourceId, sourceName, allocation, issued, claimed, revoked, createdAt) { 
        obj['id'] = id;
        obj['source_type'] = sourceType;
        obj['source_id'] = sourceId;
        obj['source_name'] = sourceName;
        obj['allocation'] = allocation;
        obj['issued'] = issued;
        obj['claimed'] = claimed;
        obj['revoked'] = revoked;
        obj['created_at'] = createdAt;
    }

    /**
     * Constructs a <code>PromoLinkBatchSummary</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PromoLinkBatchSummary} obj Optional instance to populate.
     * @return {module:model/PromoLinkBatchSummary} The populated <code>PromoLinkBatchSummary</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PromoLinkBatchSummary();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('source_type')) {
                obj['source_type'] = PromoSourceType.constructFromObject(data['source_type']);
            }
            if (data.hasOwnProperty('source_id')) {
                obj['source_id'] = ApiClient.convertToType(data['source_id'], 'String');
            }
            if (data.hasOwnProperty('source_name')) {
                obj['source_name'] = ApiClient.convertToType(data['source_name'], 'String');
            }
            if (data.hasOwnProperty('allocation')) {
                obj['allocation'] = ApiClient.convertToType(data['allocation'], 'Number');
            }
            if (data.hasOwnProperty('issued')) {
                obj['issued'] = ApiClient.convertToType(data['issued'], 'Number');
            }
            if (data.hasOwnProperty('claimed')) {
                obj['claimed'] = ApiClient.convertToType(data['claimed'], 'Number');
            }
            if (data.hasOwnProperty('revoked')) {
                obj['revoked'] = ApiClient.convertToType(data['revoked'], 'Number');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PromoLinkBatchSummary</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PromoLinkBatchSummary</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PromoLinkBatchSummary.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['source_id'] && !(typeof data['source_id'] === 'string' || data['source_id'] instanceof String)) {
            throw new Error("Expected the field `source_id` to be a primitive type in the JSON string but got " + data['source_id']);
        }
        // ensure the json data is a string
        if (data['source_name'] && !(typeof data['source_name'] === 'string' || data['source_name'] instanceof String)) {
            throw new Error("Expected the field `source_name` to be a primitive type in the JSON string but got " + data['source_name']);
        }

        return true;
    }


}

PromoLinkBatchSummary.RequiredProperties = ["id", "source_type", "source_id", "source_name", "allocation", "issued", "claimed", "revoked", "created_at"];

/**
 * @member {String} id
 */
PromoLinkBatchSummary.prototype['id'] = undefined;

/**
 * @member {module:model/PromoSourceType} source_type
 */
PromoLinkBatchSummary.prototype['source_type'] = undefined;

/**
 * @member {String} source_id
 */
PromoLinkBatchSummary.prototype['source_id'] = undefined;

/**
 * @member {String} source_name
 */
PromoLinkBatchSummary.prototype['source_name'] = undefined;

/**
 * @member {Number} allocation
 */
PromoLinkBatchSummary.prototype['allocation'] = undefined;

/**
 * @member {Number} issued
 */
PromoLinkBatchSummary.prototype['issued'] = undefined;

/**
 * @member {Number} claimed
 */
PromoLinkBatchSummary.prototype['claimed'] = undefined;

/**
 * @member {Number} revoked
 */
PromoLinkBatchSummary.prototype['revoked'] = undefined;

/**
 * @member {Date} created_at
 */
PromoLinkBatchSummary.prototype['created_at'] = undefined;






export default PromoLinkBatchSummary;

