/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RealizedPnlSettlement model module.
 * @module model/RealizedPnlSettlement
 * @version 1.0.0
 */
class RealizedPnlSettlement {
    /**
     * Constructs a new <code>RealizedPnlSettlement</code>.
     * @alias module:model/RealizedPnlSettlement
     * @param id {String} The ID of the realized PnL settlement
     * @param userId {String} The ID of the user associated with the realized PnL settlement
     * @param tenantId {String} The ID of the tenant associated with the realized PnL settlement
     * @param positionId {String} The ID of the position associated with the realized PnL settlement
     * @param orderId {String} The ID of the position-closing order associated with the realized PnL settlement
     * @param realizedUsd {String} The amount of realized PnL in USD
     * @param createdAt {Date} The timestamp when the realized PnL settlement was created
     */
    constructor(id, userId, tenantId, positionId, orderId, realizedUsd, createdAt) { 
        
        RealizedPnlSettlement.initialize(this, id, userId, tenantId, positionId, orderId, realizedUsd, createdAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, userId, tenantId, positionId, orderId, realizedUsd, createdAt) { 
        obj['id'] = id;
        obj['user_id'] = userId;
        obj['tenant_id'] = tenantId;
        obj['position_id'] = positionId;
        obj['order_id'] = orderId;
        obj['realized_usd'] = realizedUsd;
        obj['created_at'] = createdAt;
    }

    /**
     * Constructs a <code>RealizedPnlSettlement</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RealizedPnlSettlement} obj Optional instance to populate.
     * @return {module:model/RealizedPnlSettlement} The populated <code>RealizedPnlSettlement</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RealizedPnlSettlement();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('tenant_id')) {
                obj['tenant_id'] = ApiClient.convertToType(data['tenant_id'], 'String');
            }
            if (data.hasOwnProperty('position_id')) {
                obj['position_id'] = ApiClient.convertToType(data['position_id'], 'String');
            }
            if (data.hasOwnProperty('order_id')) {
                obj['order_id'] = ApiClient.convertToType(data['order_id'], 'String');
            }
            if (data.hasOwnProperty('realized_usd')) {
                obj['realized_usd'] = ApiClient.convertToType(data['realized_usd'], 'String');
            }
            if (data.hasOwnProperty('settled_at')) {
                obj['settled_at'] = ApiClient.convertToType(data['settled_at'], 'Date');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RealizedPnlSettlement</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RealizedPnlSettlement</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RealizedPnlSettlement.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['tenant_id'] && !(typeof data['tenant_id'] === 'string' || data['tenant_id'] instanceof String)) {
            throw new Error("Expected the field `tenant_id` to be a primitive type in the JSON string but got " + data['tenant_id']);
        }
        // ensure the json data is a string
        if (data['position_id'] && !(typeof data['position_id'] === 'string' || data['position_id'] instanceof String)) {
            throw new Error("Expected the field `position_id` to be a primitive type in the JSON string but got " + data['position_id']);
        }
        // ensure the json data is a string
        if (data['order_id'] && !(typeof data['order_id'] === 'string' || data['order_id'] instanceof String)) {
            throw new Error("Expected the field `order_id` to be a primitive type in the JSON string but got " + data['order_id']);
        }
        // ensure the json data is a string
        if (data['realized_usd'] && !(typeof data['realized_usd'] === 'string' || data['realized_usd'] instanceof String)) {
            throw new Error("Expected the field `realized_usd` to be a primitive type in the JSON string but got " + data['realized_usd']);
        }

        return true;
    }


}

RealizedPnlSettlement.RequiredProperties = ["id", "user_id", "tenant_id", "position_id", "order_id", "realized_usd", "created_at"];

/**
 * The ID of the realized PnL settlement
 * @member {String} id
 */
RealizedPnlSettlement.prototype['id'] = undefined;

/**
 * The ID of the user associated with the realized PnL settlement
 * @member {String} user_id
 */
RealizedPnlSettlement.prototype['user_id'] = undefined;

/**
 * The ID of the tenant associated with the realized PnL settlement
 * @member {String} tenant_id
 */
RealizedPnlSettlement.prototype['tenant_id'] = undefined;

/**
 * The ID of the position associated with the realized PnL settlement
 * @member {String} position_id
 */
RealizedPnlSettlement.prototype['position_id'] = undefined;

/**
 * The ID of the position-closing order associated with the realized PnL settlement
 * @member {String} order_id
 */
RealizedPnlSettlement.prototype['order_id'] = undefined;

/**
 * The amount of realized PnL in USD
 * @member {String} realized_usd
 */
RealizedPnlSettlement.prototype['realized_usd'] = undefined;

/**
 * The timestamp when the realized PnL settlement was settled
 * @member {Date} settled_at
 */
RealizedPnlSettlement.prototype['settled_at'] = undefined;

/**
 * The timestamp when the realized PnL settlement was created
 * @member {Date} created_at
 */
RealizedPnlSettlement.prototype['created_at'] = undefined;






export default RealizedPnlSettlement;

