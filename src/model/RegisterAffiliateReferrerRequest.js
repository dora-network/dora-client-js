/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RegisterAffiliateReferrerRequest model module.
 * @module model/RegisterAffiliateReferrerRequest
 * @version 1.0.0
 */
class RegisterAffiliateReferrerRequest {
    /**
     * Constructs a new <code>RegisterAffiliateReferrerRequest</code>.
     * @alias module:model/RegisterAffiliateReferrerRequest
     * @param userId {String} Existing, nonzero DORA user ID in the program owning tenant.
     */
    constructor(userId) { 
        
        RegisterAffiliateReferrerRequest.initialize(this, userId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, userId) { 
        obj['user_id'] = userId;
    }

    /**
     * Constructs a <code>RegisterAffiliateReferrerRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RegisterAffiliateReferrerRequest} obj Optional instance to populate.
     * @return {module:model/RegisterAffiliateReferrerRequest} The populated <code>RegisterAffiliateReferrerRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RegisterAffiliateReferrerRequest();

            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('referral_code')) {
                obj['referral_code'] = ApiClient.convertToType(data['referral_code'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RegisterAffiliateReferrerRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RegisterAffiliateReferrerRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RegisterAffiliateReferrerRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['referral_code'] && !(typeof data['referral_code'] === 'string' || data['referral_code'] instanceof String)) {
            throw new Error("Expected the field `referral_code` to be a primitive type in the JSON string but got " + data['referral_code']);
        }

        return true;
    }


}

RegisterAffiliateReferrerRequest.RequiredProperties = ["user_id"];

/**
 * Existing, nonzero DORA user ID in the program owning tenant.
 * @member {String} user_id
 */
RegisterAffiliateReferrerRequest.prototype['user_id'] = undefined;

/**
 * Optional custom code: 3 to 64 letters, digits, hyphens or underscores after trimming, starting with a letter or digit. Stored uppercase and unique across all programs and tenants. Omitted, null or empty generates a random code.
 * @member {String} referral_code
 */
RegisterAffiliateReferrerRequest.prototype['referral_code'] = undefined;






export default RegisterAffiliateReferrerRequest;

