/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The RevokeAPIKeyData model module.
 * @module model/RevokeAPIKeyData
 * @version 1.0.0
 */
class RevokeAPIKeyData {
    /**
     * Constructs a new <code>RevokeAPIKeyData</code>.
     * @alias module:model/RevokeAPIKeyData
     * @param userId {String} 
     * @param keyId {String} 
     * @param label {String} 
     * @param isActive {Boolean} 
     */
    constructor(userId, keyId, label, isActive) { 
        
        RevokeAPIKeyData.initialize(this, userId, keyId, label, isActive);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, userId, keyId, label, isActive) { 
        obj['user_id'] = userId;
        obj['key_id'] = keyId;
        obj['label'] = label;
        obj['is_active'] = isActive;
    }

    /**
     * Constructs a <code>RevokeAPIKeyData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/RevokeAPIKeyData} obj Optional instance to populate.
     * @return {module:model/RevokeAPIKeyData} The populated <code>RevokeAPIKeyData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new RevokeAPIKeyData();

            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('key_id')) {
                obj['key_id'] = ApiClient.convertToType(data['key_id'], 'String');
            }
            if (data.hasOwnProperty('label')) {
                obj['label'] = ApiClient.convertToType(data['label'], 'String');
            }
            if (data.hasOwnProperty('is_active')) {
                obj['is_active'] = ApiClient.convertToType(data['is_active'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>RevokeAPIKeyData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>RevokeAPIKeyData</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of RevokeAPIKeyData.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['key_id'] && !(typeof data['key_id'] === 'string' || data['key_id'] instanceof String)) {
            throw new Error("Expected the field `key_id` to be a primitive type in the JSON string but got " + data['key_id']);
        }
        // ensure the json data is a string
        if (data['label'] && !(typeof data['label'] === 'string' || data['label'] instanceof String)) {
            throw new Error("Expected the field `label` to be a primitive type in the JSON string but got " + data['label']);
        }

        return true;
    }


}

RevokeAPIKeyData.RequiredProperties = ["user_id", "key_id", "label", "is_active"];

/**
 * @member {String} user_id
 */
RevokeAPIKeyData.prototype['user_id'] = undefined;

/**
 * @member {String} key_id
 */
RevokeAPIKeyData.prototype['key_id'] = undefined;

/**
 * @member {String} label
 */
RevokeAPIKeyData.prototype['label'] = undefined;

/**
 * @member {Boolean} is_active
 */
RevokeAPIKeyData.prototype['is_active'] = undefined;






export default RevokeAPIKeyData;

