/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SettleLeverageAccruedInterest model module.
 * @module model/SettleLeverageAccruedInterest
 * @version 1.0.0
 */
class SettleLeverageAccruedInterest {
    /**
     * Constructs a new <code>SettleLeverageAccruedInterest</code>.
     * @alias module:model/SettleLeverageAccruedInterest
     * @param transactionId {String} 
     * @param assetId {String} 
     * @param positionId {String} 
     * @param currentAccruedInterestUsd {String} 
     */
    constructor(transactionId, assetId, positionId, currentAccruedInterestUsd) { 
        
        SettleLeverageAccruedInterest.initialize(this, transactionId, assetId, positionId, currentAccruedInterestUsd);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, transactionId, assetId, positionId, currentAccruedInterestUsd) { 
        obj['transaction_id'] = transactionId;
        obj['asset_id'] = assetId;
        obj['position_id'] = positionId;
        obj['current_accrued_interest_usd'] = currentAccruedInterestUsd;
    }

    /**
     * Constructs a <code>SettleLeverageAccruedInterest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SettleLeverageAccruedInterest} obj Optional instance to populate.
     * @return {module:model/SettleLeverageAccruedInterest} The populated <code>SettleLeverageAccruedInterest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SettleLeverageAccruedInterest();

            if (data.hasOwnProperty('transaction_id')) {
                obj['transaction_id'] = ApiClient.convertToType(data['transaction_id'], 'String');
            }
            if (data.hasOwnProperty('asset_id')) {
                obj['asset_id'] = ApiClient.convertToType(data['asset_id'], 'String');
            }
            if (data.hasOwnProperty('position_id')) {
                obj['position_id'] = ApiClient.convertToType(data['position_id'], 'String');
            }
            if (data.hasOwnProperty('current_accrued_interest_usd')) {
                obj['current_accrued_interest_usd'] = ApiClient.convertToType(data['current_accrued_interest_usd'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SettleLeverageAccruedInterest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SettleLeverageAccruedInterest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SettleLeverageAccruedInterest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['transaction_id'] && !(typeof data['transaction_id'] === 'string' || data['transaction_id'] instanceof String)) {
            throw new Error("Expected the field `transaction_id` to be a primitive type in the JSON string but got " + data['transaction_id']);
        }
        // ensure the json data is a string
        if (data['asset_id'] && !(typeof data['asset_id'] === 'string' || data['asset_id'] instanceof String)) {
            throw new Error("Expected the field `asset_id` to be a primitive type in the JSON string but got " + data['asset_id']);
        }
        // ensure the json data is a string
        if (data['position_id'] && !(typeof data['position_id'] === 'string' || data['position_id'] instanceof String)) {
            throw new Error("Expected the field `position_id` to be a primitive type in the JSON string but got " + data['position_id']);
        }
        // ensure the json data is a string
        if (data['current_accrued_interest_usd'] && !(typeof data['current_accrued_interest_usd'] === 'string' || data['current_accrued_interest_usd'] instanceof String)) {
            throw new Error("Expected the field `current_accrued_interest_usd` to be a primitive type in the JSON string but got " + data['current_accrued_interest_usd']);
        }

        return true;
    }


}

SettleLeverageAccruedInterest.RequiredProperties = ["transaction_id", "asset_id", "position_id", "current_accrued_interest_usd"];

/**
 * @member {String} transaction_id
 */
SettleLeverageAccruedInterest.prototype['transaction_id'] = undefined;

/**
 * @member {String} asset_id
 */
SettleLeverageAccruedInterest.prototype['asset_id'] = undefined;

/**
 * @member {String} position_id
 */
SettleLeverageAccruedInterest.prototype['position_id'] = undefined;

/**
 * @member {String} current_accrued_interest_usd
 */
SettleLeverageAccruedInterest.prototype['current_accrued_interest_usd'] = undefined;






export default SettleLeverageAccruedInterest;

