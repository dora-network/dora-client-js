/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Supply model module.
 * @module model/Supply
 * @version 1.0.0
 */
class Supply {
    /**
     * Constructs a new <code>Supply</code>.
     * @alias module:model/Supply
     * @param positionId {String} 
     * @param transactionId {String} 
     * @param assetId {String} 
     * @param quantity {String} 
     */
    constructor(positionId, transactionId, assetId, quantity) { 
        
        Supply.initialize(this, positionId, transactionId, assetId, quantity);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, positionId, transactionId, assetId, quantity) { 
        obj['position_id'] = positionId;
        obj['transaction_id'] = transactionId;
        obj['asset_id'] = assetId;
        obj['quantity'] = quantity;
    }

    /**
     * Constructs a <code>Supply</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Supply} obj Optional instance to populate.
     * @return {module:model/Supply} The populated <code>Supply</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Supply();

            if (data.hasOwnProperty('position_id')) {
                obj['position_id'] = ApiClient.convertToType(data['position_id'], 'String');
            }
            if (data.hasOwnProperty('transaction_id')) {
                obj['transaction_id'] = ApiClient.convertToType(data['transaction_id'], 'String');
            }
            if (data.hasOwnProperty('asset_id')) {
                obj['asset_id'] = ApiClient.convertToType(data['asset_id'], 'String');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Supply</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Supply</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Supply.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['position_id'] && !(typeof data['position_id'] === 'string' || data['position_id'] instanceof String)) {
            throw new Error("Expected the field `position_id` to be a primitive type in the JSON string but got " + data['position_id']);
        }
        // ensure the json data is a string
        if (data['transaction_id'] && !(typeof data['transaction_id'] === 'string' || data['transaction_id'] instanceof String)) {
            throw new Error("Expected the field `transaction_id` to be a primitive type in the JSON string but got " + data['transaction_id']);
        }
        // ensure the json data is a string
        if (data['asset_id'] && !(typeof data['asset_id'] === 'string' || data['asset_id'] instanceof String)) {
            throw new Error("Expected the field `asset_id` to be a primitive type in the JSON string but got " + data['asset_id']);
        }
        // ensure the json data is a string
        if (data['quantity'] && !(typeof data['quantity'] === 'string' || data['quantity'] instanceof String)) {
            throw new Error("Expected the field `quantity` to be a primitive type in the JSON string but got " + data['quantity']);
        }

        return true;
    }


}

Supply.RequiredProperties = ["position_id", "transaction_id", "asset_id", "quantity"];

/**
 * @member {String} position_id
 */
Supply.prototype['position_id'] = undefined;

/**
 * @member {String} transaction_id
 */
Supply.prototype['transaction_id'] = undefined;

/**
 * @member {String} asset_id
 */
Supply.prototype['asset_id'] = undefined;

/**
 * @member {String} quantity
 */
Supply.prototype['quantity'] = undefined;






export default Supply;

