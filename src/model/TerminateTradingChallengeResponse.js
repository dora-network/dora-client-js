/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TerminateTradingChallengeResponse model module.
 * @module model/TerminateTradingChallengeResponse
 * @version 1.0.0
 */
class TerminateTradingChallengeResponse {
    /**
     * Constructs a new <code>TerminateTradingChallengeResponse</code>.
     * @alias module:model/TerminateTradingChallengeResponse
     * @param tradingChallengeId {String} 
     * @param userId {String} 
     * @param status {module:model/TerminateTradingChallengeResponse.StatusEnum} 
     */
    constructor(tradingChallengeId, userId, status) { 
        
        TerminateTradingChallengeResponse.initialize(this, tradingChallengeId, userId, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, tradingChallengeId, userId, status) { 
        obj['trading_challenge_id'] = tradingChallengeId;
        obj['user_id'] = userId;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>TerminateTradingChallengeResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TerminateTradingChallengeResponse} obj Optional instance to populate.
     * @return {module:model/TerminateTradingChallengeResponse} The populated <code>TerminateTradingChallengeResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TerminateTradingChallengeResponse();

            if (data.hasOwnProperty('trading_challenge_id')) {
                obj['trading_challenge_id'] = ApiClient.convertToType(data['trading_challenge_id'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TerminateTradingChallengeResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TerminateTradingChallengeResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TerminateTradingChallengeResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['trading_challenge_id'] && !(typeof data['trading_challenge_id'] === 'string' || data['trading_challenge_id'] instanceof String)) {
            throw new Error("Expected the field `trading_challenge_id` to be a primitive type in the JSON string but got " + data['trading_challenge_id']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

TerminateTradingChallengeResponse.RequiredProperties = ["trading_challenge_id", "user_id", "status"];

/**
 * @member {String} trading_challenge_id
 */
TerminateTradingChallengeResponse.prototype['trading_challenge_id'] = undefined;

/**
 * @member {String} user_id
 */
TerminateTradingChallengeResponse.prototype['user_id'] = undefined;

/**
 * @member {module:model/TerminateTradingChallengeResponse.StatusEnum} status
 */
TerminateTradingChallengeResponse.prototype['status'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
TerminateTradingChallengeResponse['StatusEnum'] = {

    /**
     * value: "TERMINATED"
     * @const
     */
    "TERMINATED": "TERMINATED"
};



export default TerminateTradingChallengeResponse;

