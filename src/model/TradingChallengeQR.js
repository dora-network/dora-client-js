/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The TradingChallengeQR model module.
 * @module model/TradingChallengeQR
 * @version 1.0.0
 */
class TradingChallengeQR {
    /**
     * Constructs a new <code>TradingChallengeQR</code>.
     * @alias module:model/TradingChallengeQR
     * @param minRewardAmount {String} 
     * @param maxRewardAmount {String} 
     * @param rewardClaimGraceDays {Number} 
     * @param claimedLinksCount {Number} 
     * @param issuedLinksCount {Number} 
     */
    constructor(minRewardAmount, maxRewardAmount, rewardClaimGraceDays, claimedLinksCount, issuedLinksCount) { 
        
        TradingChallengeQR.initialize(this, minRewardAmount, maxRewardAmount, rewardClaimGraceDays, claimedLinksCount, issuedLinksCount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, minRewardAmount, maxRewardAmount, rewardClaimGraceDays, claimedLinksCount, issuedLinksCount) { 
        obj['min_reward_amount'] = minRewardAmount;
        obj['max_reward_amount'] = maxRewardAmount;
        obj['reward_claim_grace_days'] = rewardClaimGraceDays;
        obj['claimed_links_count'] = claimedLinksCount;
        obj['issued_links_count'] = issuedLinksCount;
    }

    /**
     * Constructs a <code>TradingChallengeQR</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TradingChallengeQR} obj Optional instance to populate.
     * @return {module:model/TradingChallengeQR} The populated <code>TradingChallengeQR</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TradingChallengeQR();

            if (data.hasOwnProperty('min_reward_amount')) {
                obj['min_reward_amount'] = ApiClient.convertToType(data['min_reward_amount'], 'String');
            }
            if (data.hasOwnProperty('max_reward_amount')) {
                obj['max_reward_amount'] = ApiClient.convertToType(data['max_reward_amount'], 'String');
            }
            if (data.hasOwnProperty('reward_claim_grace_days')) {
                obj['reward_claim_grace_days'] = ApiClient.convertToType(data['reward_claim_grace_days'], 'Number');
            }
            if (data.hasOwnProperty('claimed_links_count')) {
                obj['claimed_links_count'] = ApiClient.convertToType(data['claimed_links_count'], 'Number');
            }
            if (data.hasOwnProperty('issued_links_count')) {
                obj['issued_links_count'] = ApiClient.convertToType(data['issued_links_count'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TradingChallengeQR</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TradingChallengeQR</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TradingChallengeQR.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['min_reward_amount'] && !(typeof data['min_reward_amount'] === 'string' || data['min_reward_amount'] instanceof String)) {
            throw new Error("Expected the field `min_reward_amount` to be a primitive type in the JSON string but got " + data['min_reward_amount']);
        }
        // ensure the json data is a string
        if (data['max_reward_amount'] && !(typeof data['max_reward_amount'] === 'string' || data['max_reward_amount'] instanceof String)) {
            throw new Error("Expected the field `max_reward_amount` to be a primitive type in the JSON string but got " + data['max_reward_amount']);
        }

        return true;
    }


}

TradingChallengeQR.RequiredProperties = ["min_reward_amount", "max_reward_amount", "reward_claim_grace_days", "claimed_links_count", "issued_links_count"];

/**
 * @member {String} min_reward_amount
 */
TradingChallengeQR.prototype['min_reward_amount'] = undefined;

/**
 * @member {String} max_reward_amount
 */
TradingChallengeQR.prototype['max_reward_amount'] = undefined;

/**
 * @member {Number} reward_claim_grace_days
 */
TradingChallengeQR.prototype['reward_claim_grace_days'] = undefined;

/**
 * @member {Number} claimed_links_count
 */
TradingChallengeQR.prototype['claimed_links_count'] = undefined;

/**
 * @member {Number} issued_links_count
 */
TradingChallengeQR.prototype['issued_links_count'] = undefined;






export default TradingChallengeQR;

