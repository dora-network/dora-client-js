/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Side from './Side';
import TransactionKind from './TransactionKind';

/**
 * The Transaction model module.
 * @module model/Transaction
 * @version 1.0.0
 */
class Transaction {
    /**
     * Constructs a new <code>Transaction</code>.
     * @alias module:model/Transaction
     * @param id {String} 
     * @param createdAt {Date} 
     * @param kind {module:model/TransactionKind} 
     * @param asset0 {String} 
     * @param quantity0 {String} 
     * @param quantity1 {String} 
     * @param asset1 {String} 
     * @param userId {String} 
     * @param adminUserId {String} 
     * @param orderSide {module:model/Side} 
     */
    constructor(id, createdAt, kind, asset0, quantity0, quantity1, asset1, userId, adminUserId, orderSide) { 
        
        Transaction.initialize(this, id, createdAt, kind, asset0, quantity0, quantity1, asset1, userId, adminUserId, orderSide);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, createdAt, kind, asset0, quantity0, quantity1, asset1, userId, adminUserId, orderSide) { 
        obj['id'] = id;
        obj['created_at'] = createdAt;
        obj['kind'] = kind;
        obj['asset0'] = asset0;
        obj['quantity0'] = quantity0;
        obj['quantity1'] = quantity1;
        obj['asset1'] = asset1;
        obj['user_id'] = userId;
        obj['admin_user_id'] = adminUserId;
        obj['order_side'] = orderSide;
    }

    /**
     * Constructs a <code>Transaction</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Transaction} obj Optional instance to populate.
     * @return {module:model/Transaction} The populated <code>Transaction</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Transaction();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = TransactionKind.constructFromObject(data['kind']);
            }
            if (data.hasOwnProperty('asset0')) {
                obj['asset0'] = ApiClient.convertToType(data['asset0'], 'String');
            }
            if (data.hasOwnProperty('quantity0')) {
                obj['quantity0'] = ApiClient.convertToType(data['quantity0'], 'String');
            }
            if (data.hasOwnProperty('quantity1')) {
                obj['quantity1'] = ApiClient.convertToType(data['quantity1'], 'String');
            }
            if (data.hasOwnProperty('asset1')) {
                obj['asset1'] = ApiClient.convertToType(data['asset1'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('admin_user_id')) {
                obj['admin_user_id'] = ApiClient.convertToType(data['admin_user_id'], 'String');
            }
            if (data.hasOwnProperty('order_side')) {
                obj['order_side'] = Side.constructFromObject(data['order_side']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Transaction</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Transaction</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Transaction.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['asset0'] && !(typeof data['asset0'] === 'string' || data['asset0'] instanceof String)) {
            throw new Error("Expected the field `asset0` to be a primitive type in the JSON string but got " + data['asset0']);
        }
        // ensure the json data is a string
        if (data['quantity0'] && !(typeof data['quantity0'] === 'string' || data['quantity0'] instanceof String)) {
            throw new Error("Expected the field `quantity0` to be a primitive type in the JSON string but got " + data['quantity0']);
        }
        // ensure the json data is a string
        if (data['quantity1'] && !(typeof data['quantity1'] === 'string' || data['quantity1'] instanceof String)) {
            throw new Error("Expected the field `quantity1` to be a primitive type in the JSON string but got " + data['quantity1']);
        }
        // ensure the json data is a string
        if (data['asset1'] && !(typeof data['asset1'] === 'string' || data['asset1'] instanceof String)) {
            throw new Error("Expected the field `asset1` to be a primitive type in the JSON string but got " + data['asset1']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['admin_user_id'] && !(typeof data['admin_user_id'] === 'string' || data['admin_user_id'] instanceof String)) {
            throw new Error("Expected the field `admin_user_id` to be a primitive type in the JSON string but got " + data['admin_user_id']);
        }

        return true;
    }


}

Transaction.RequiredProperties = ["id", "created_at", "kind", "asset0", "quantity0", "quantity1", "asset1", "user_id", "admin_user_id", "order_side"];

/**
 * @member {String} id
 */
Transaction.prototype['id'] = undefined;

/**
 * @member {Date} created_at
 */
Transaction.prototype['created_at'] = undefined;

/**
 * @member {module:model/TransactionKind} kind
 */
Transaction.prototype['kind'] = undefined;

/**
 * @member {String} asset0
 */
Transaction.prototype['asset0'] = undefined;

/**
 * @member {String} quantity0
 */
Transaction.prototype['quantity0'] = undefined;

/**
 * @member {String} quantity1
 */
Transaction.prototype['quantity1'] = undefined;

/**
 * @member {String} asset1
 */
Transaction.prototype['asset1'] = undefined;

/**
 * @member {String} user_id
 */
Transaction.prototype['user_id'] = undefined;

/**
 * @member {String} admin_user_id
 */
Transaction.prototype['admin_user_id'] = undefined;

/**
 * @member {module:model/Side} order_side
 */
Transaction.prototype['order_side'] = undefined;






export default Transaction;

