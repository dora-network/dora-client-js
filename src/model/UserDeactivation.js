/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UserDeactivation model module.
 * @module model/UserDeactivation
 * @version 1.0.0
 */
class UserDeactivation {
    /**
     * Constructs a new <code>UserDeactivation</code>.
     * An admin-requested account deactivation. A non-REACTIVATED request blocks the user&#39;s trading, funding, and transfers.
     * @alias module:model/UserDeactivation
     * @param deactivationId {String} 
     * @param userId {String} 
     * @param requestedBy {String} Admin that requested the deactivation.
     * @param reason {String} 
     * @param status {module:model/UserDeactivation.StatusEnum} PENDING: wind-down in progress. FAILED: wind-down gave up; admin can re-trigger. COMPLETED: account deactivated. REACTIVATED: blocks lifted.
     * @param attempts {Number} Wind-down attempts performed so far.
     * @param createdAt {Date} 
     * @param updatedAt {Date} 
     */
    constructor(deactivationId, userId, requestedBy, reason, status, attempts, createdAt, updatedAt) { 
        
        UserDeactivation.initialize(this, deactivationId, userId, requestedBy, reason, status, attempts, createdAt, updatedAt);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, deactivationId, userId, requestedBy, reason, status, attempts, createdAt, updatedAt) { 
        obj['deactivation_id'] = deactivationId;
        obj['user_id'] = userId;
        obj['requested_by'] = requestedBy;
        obj['reason'] = reason;
        obj['status'] = status;
        obj['attempts'] = attempts;
        obj['created_at'] = createdAt;
        obj['updated_at'] = updatedAt;
    }

    /**
     * Constructs a <code>UserDeactivation</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UserDeactivation} obj Optional instance to populate.
     * @return {module:model/UserDeactivation} The populated <code>UserDeactivation</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UserDeactivation();

            if (data.hasOwnProperty('deactivation_id')) {
                obj['deactivation_id'] = ApiClient.convertToType(data['deactivation_id'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('requested_by')) {
                obj['requested_by'] = ApiClient.convertToType(data['requested_by'], 'String');
            }
            if (data.hasOwnProperty('reason')) {
                obj['reason'] = ApiClient.convertToType(data['reason'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('attempts')) {
                obj['attempts'] = ApiClient.convertToType(data['attempts'], 'Number');
            }
            if (data.hasOwnProperty('result')) {
                obj['result'] = ApiClient.convertToType(data['result'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
            if (data.hasOwnProperty('completed_at')) {
                obj['completed_at'] = ApiClient.convertToType(data['completed_at'], 'Date');
            }
            if (data.hasOwnProperty('reactivated_by')) {
                obj['reactivated_by'] = ApiClient.convertToType(data['reactivated_by'], 'String');
            }
            if (data.hasOwnProperty('reactivated_at')) {
                obj['reactivated_at'] = ApiClient.convertToType(data['reactivated_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UserDeactivation</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UserDeactivation</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UserDeactivation.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['deactivation_id'] && !(typeof data['deactivation_id'] === 'string' || data['deactivation_id'] instanceof String)) {
            throw new Error("Expected the field `deactivation_id` to be a primitive type in the JSON string but got " + data['deactivation_id']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['requested_by'] && !(typeof data['requested_by'] === 'string' || data['requested_by'] instanceof String)) {
            throw new Error("Expected the field `requested_by` to be a primitive type in the JSON string but got " + data['requested_by']);
        }
        // ensure the json data is a string
        if (data['reason'] && !(typeof data['reason'] === 'string' || data['reason'] instanceof String)) {
            throw new Error("Expected the field `reason` to be a primitive type in the JSON string but got " + data['reason']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['result'] && !(typeof data['result'] === 'string' || data['result'] instanceof String)) {
            throw new Error("Expected the field `result` to be a primitive type in the JSON string but got " + data['result']);
        }
        // ensure the json data is a string
        if (data['reactivated_by'] && !(typeof data['reactivated_by'] === 'string' || data['reactivated_by'] instanceof String)) {
            throw new Error("Expected the field `reactivated_by` to be a primitive type in the JSON string but got " + data['reactivated_by']);
        }

        return true;
    }


}

UserDeactivation.RequiredProperties = ["deactivation_id", "user_id", "requested_by", "reason", "status", "attempts", "created_at", "updated_at"];

/**
 * @member {String} deactivation_id
 */
UserDeactivation.prototype['deactivation_id'] = undefined;

/**
 * @member {String} user_id
 */
UserDeactivation.prototype['user_id'] = undefined;

/**
 * Admin that requested the deactivation.
 * @member {String} requested_by
 */
UserDeactivation.prototype['requested_by'] = undefined;

/**
 * @member {String} reason
 */
UserDeactivation.prototype['reason'] = undefined;

/**
 * PENDING: wind-down in progress. FAILED: wind-down gave up; admin can re-trigger. COMPLETED: account deactivated. REACTIVATED: blocks lifted.
 * @member {module:model/UserDeactivation.StatusEnum} status
 */
UserDeactivation.prototype['status'] = undefined;

/**
 * Wind-down attempts performed so far.
 * @member {Number} attempts
 */
UserDeactivation.prototype['attempts'] = undefined;

/**
 * Latest wind-down outcome or error summary.
 * @member {String} result
 */
UserDeactivation.prototype['result'] = undefined;

/**
 * @member {Date} created_at
 */
UserDeactivation.prototype['created_at'] = undefined;

/**
 * @member {Date} updated_at
 */
UserDeactivation.prototype['updated_at'] = undefined;

/**
 * @member {Date} completed_at
 */
UserDeactivation.prototype['completed_at'] = undefined;

/**
 * @member {String} reactivated_by
 */
UserDeactivation.prototype['reactivated_by'] = undefined;

/**
 * @member {Date} reactivated_at
 */
UserDeactivation.prototype['reactivated_at'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
UserDeactivation['StatusEnum'] = {

    /**
     * value: "PENDING"
     * @const
     */
    "PENDING": "PENDING",

    /**
     * value: "FAILED"
     * @const
     */
    "FAILED": "FAILED",

    /**
     * value: "COMPLETED"
     * @const
     */
    "COMPLETED": "COMPLETED",

    /**
     * value: "REACTIVATED"
     * @const
     */
    "REACTIVATED": "REACTIVATED"
};



export default UserDeactivation;

