/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AssetConfig from './AssetConfig';
import OrderKind from './OrderKind';
import PositionAsset from './PositionAsset';
import Side from './Side';
import TenantRestrictions from './TenantRestrictions';

/**
 * The ValidateSubmitOrderRequest model module.
 * @module model/ValidateSubmitOrderRequest
 * @version 1.0.0
 */
class ValidateSubmitOrderRequest {
    /**
     * Constructs a new <code>ValidateSubmitOrderRequest</code>.
     * @alias module:model/ValidateSubmitOrderRequest
     * @param quantity {String} 
     * @param tick {String} Minimum tradable increment for the selected order book
     * @param kind {module:model/OrderKind} Must be LIMIT or MARKET
     * @param price {String} If kind is LIMIT, must be > 0; if MARKET it must be 0 or omitted
     * @param inverseLeverage {String} 
     * @param userBalance {String} User balance used to ensure they can afford the requested quantity
     */
    constructor(quantity, tick, kind, price, inverseLeverage, userBalance) { 
        
        ValidateSubmitOrderRequest.initialize(this, quantity, tick, kind, price, inverseLeverage, userBalance);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, quantity, tick, kind, price, inverseLeverage, userBalance) { 
        obj['quantity'] = quantity;
        obj['tick'] = tick;
        obj['kind'] = kind;
        obj['price'] = price;
        obj['inverse_leverage'] = inverseLeverage;
        obj['user_balance'] = userBalance;
    }

    /**
     * Constructs a <code>ValidateSubmitOrderRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ValidateSubmitOrderRequest} obj Optional instance to populate.
     * @return {module:model/ValidateSubmitOrderRequest} The populated <code>ValidateSubmitOrderRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ValidateSubmitOrderRequest();

            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'String');
            }
            if (data.hasOwnProperty('tick')) {
                obj['tick'] = ApiClient.convertToType(data['tick'], 'String');
            }
            if (data.hasOwnProperty('kind')) {
                obj['kind'] = ApiClient.convertToType(data['kind'], OrderKind);
            }
            if (data.hasOwnProperty('side')) {
                obj['side'] = ApiClient.convertToType(data['side'], Side);
            }
            if (data.hasOwnProperty('price')) {
                obj['price'] = ApiClient.convertToType(data['price'], 'String');
            }
            if (data.hasOwnProperty('good_till_date')) {
                obj['good_till_date'] = ApiClient.convertToType(data['good_till_date'], 'Date');
            }
            if (data.hasOwnProperty('inverse_leverage')) {
                obj['inverse_leverage'] = ApiClient.convertToType(data['inverse_leverage'], 'String');
            }
            if (data.hasOwnProperty('user_balance')) {
                obj['user_balance'] = ApiClient.convertToType(data['user_balance'], 'String');
            }
            if (data.hasOwnProperty('base_asset_id')) {
                obj['base_asset_id'] = ApiClient.convertToType(data['base_asset_id'], 'String');
            }
            if (data.hasOwnProperty('quote_asset_id')) {
                obj['quote_asset_id'] = ApiClient.convertToType(data['quote_asset_id'], 'String');
            }
            if (data.hasOwnProperty('client_order_id')) {
                obj['client_order_id'] = ApiClient.convertToType(data['client_order_id'], 'String');
            }
            if (data.hasOwnProperty('position_assets')) {
                obj['position_assets'] = ApiClient.convertToType(data['position_assets'], [PositionAsset]);
            }
            if (data.hasOwnProperty('assets_config')) {
                obj['assets_config'] = ApiClient.convertToType(data['assets_config'], [AssetConfig]);
            }
            if (data.hasOwnProperty('stop_loss_price')) {
                obj['stop_loss_price'] = ApiClient.convertToType(data['stop_loss_price'], 'String');
            }
            if (data.hasOwnProperty('take_profit_price')) {
                obj['take_profit_price'] = ApiClient.convertToType(data['take_profit_price'], 'String');
            }
            if (data.hasOwnProperty('restrictions')) {
                obj['restrictions'] = TenantRestrictions.constructFromObject(data['restrictions']);
            }
            if (data.hasOwnProperty('initial_capital')) {
                obj['initial_capital'] = ApiClient.convertToType(data['initial_capital'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ValidateSubmitOrderRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ValidateSubmitOrderRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ValidateSubmitOrderRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['quantity'] && !(typeof data['quantity'] === 'string' || data['quantity'] instanceof String)) {
            throw new Error("Expected the field `quantity` to be a primitive type in the JSON string but got " + data['quantity']);
        }
        // ensure the json data is a string
        if (data['tick'] && !(typeof data['tick'] === 'string' || data['tick'] instanceof String)) {
            throw new Error("Expected the field `tick` to be a primitive type in the JSON string but got " + data['tick']);
        }
        // ensure the json data is a string
        if (data['price'] && !(typeof data['price'] === 'string' || data['price'] instanceof String)) {
            throw new Error("Expected the field `price` to be a primitive type in the JSON string but got " + data['price']);
        }
        // ensure the json data is a string
        if (data['inverse_leverage'] && !(typeof data['inverse_leverage'] === 'string' || data['inverse_leverage'] instanceof String)) {
            throw new Error("Expected the field `inverse_leverage` to be a primitive type in the JSON string but got " + data['inverse_leverage']);
        }
        // ensure the json data is a string
        if (data['user_balance'] && !(typeof data['user_balance'] === 'string' || data['user_balance'] instanceof String)) {
            throw new Error("Expected the field `user_balance` to be a primitive type in the JSON string but got " + data['user_balance']);
        }
        // ensure the json data is a string
        if (data['base_asset_id'] && !(typeof data['base_asset_id'] === 'string' || data['base_asset_id'] instanceof String)) {
            throw new Error("Expected the field `base_asset_id` to be a primitive type in the JSON string but got " + data['base_asset_id']);
        }
        // ensure the json data is a string
        if (data['quote_asset_id'] && !(typeof data['quote_asset_id'] === 'string' || data['quote_asset_id'] instanceof String)) {
            throw new Error("Expected the field `quote_asset_id` to be a primitive type in the JSON string but got " + data['quote_asset_id']);
        }
        // ensure the json data is a string
        if (data['client_order_id'] && !(typeof data['client_order_id'] === 'string' || data['client_order_id'] instanceof String)) {
            throw new Error("Expected the field `client_order_id` to be a primitive type in the JSON string but got " + data['client_order_id']);
        }
        if (data['position_assets']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['position_assets'])) {
                throw new Error("Expected the field `position_assets` to be an array in the JSON data but got " + data['position_assets']);
            }
            // validate the optional field `position_assets` (array)
            for (const item of data['position_assets']) {
                PositionAsset.validateJSON(item);
            };
        }
        if (data['assets_config']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['assets_config'])) {
                throw new Error("Expected the field `assets_config` to be an array in the JSON data but got " + data['assets_config']);
            }
            // validate the optional field `assets_config` (array)
            for (const item of data['assets_config']) {
                AssetConfig.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['stop_loss_price'] && !(typeof data['stop_loss_price'] === 'string' || data['stop_loss_price'] instanceof String)) {
            throw new Error("Expected the field `stop_loss_price` to be a primitive type in the JSON string but got " + data['stop_loss_price']);
        }
        // ensure the json data is a string
        if (data['take_profit_price'] && !(typeof data['take_profit_price'] === 'string' || data['take_profit_price'] instanceof String)) {
            throw new Error("Expected the field `take_profit_price` to be a primitive type in the JSON string but got " + data['take_profit_price']);
        }
        // validate the optional field `restrictions`
        if (data['restrictions']) { // data not null
          TenantRestrictions.validateJSON(data['restrictions']);
        }
        // ensure the json data is a string
        if (data['initial_capital'] && !(typeof data['initial_capital'] === 'string' || data['initial_capital'] instanceof String)) {
            throw new Error("Expected the field `initial_capital` to be a primitive type in the JSON string but got " + data['initial_capital']);
        }

        return true;
    }


}

ValidateSubmitOrderRequest.RequiredProperties = ["quantity", "tick", "kind", "price", "inverse_leverage", "user_balance"];

/**
 * @member {String} quantity
 */
ValidateSubmitOrderRequest.prototype['quantity'] = undefined;

/**
 * Minimum tradable increment for the selected order book
 * @member {String} tick
 */
ValidateSubmitOrderRequest.prototype['tick'] = undefined;

/**
 * Must be LIMIT or MARKET
 * @member {module:model/OrderKind} kind
 */
ValidateSubmitOrderRequest.prototype['kind'] = undefined;

/**
 * Must be BUY or SELL
 * @member {module:model/Side} side
 */
ValidateSubmitOrderRequest.prototype['side'] = undefined;

/**
 * If kind is LIMIT, must be > 0; if MARKET it must be 0 or omitted
 * @member {String} price
 */
ValidateSubmitOrderRequest.prototype['price'] = undefined;

/**
 * @member {Date} good_till_date
 */
ValidateSubmitOrderRequest.prototype['good_till_date'] = undefined;

/**
 * @member {String} inverse_leverage
 */
ValidateSubmitOrderRequest.prototype['inverse_leverage'] = undefined;

/**
 * User balance used to ensure they can afford the requested quantity
 * @member {String} user_balance
 */
ValidateSubmitOrderRequest.prototype['user_balance'] = undefined;

/**
 * base asset of orderbook
 * @member {String} base_asset_id
 */
ValidateSubmitOrderRequest.prototype['base_asset_id'] = undefined;

/**
 * quote asset of orderbook
 * @member {String} quote_asset_id
 */
ValidateSubmitOrderRequest.prototype['quote_asset_id'] = undefined;

/**
 * An optional client-provided identifier for the order.
 * @member {String} client_order_id
 */
ValidateSubmitOrderRequest.prototype['client_order_id'] = undefined;

/**
 * Full list of assets in the position with their price and collateral weight, required when inverse_leverage < 1 for leverage health checks
 * @member {Array.<module:model/PositionAsset>} position_assets
 */
ValidateSubmitOrderRequest.prototype['position_assets'] = undefined;

/**
 * Configuration for the assets in the order
 * @member {Array.<module:model/AssetConfig>} assets_config
 */
ValidateSubmitOrderRequest.prototype['assets_config'] = undefined;

/**
 * Stop loss price
 * @member {String} stop_loss_price
 */
ValidateSubmitOrderRequest.prototype['stop_loss_price'] = undefined;

/**
 * Take profit price
 * @member {String} take_profit_price
 */
ValidateSubmitOrderRequest.prototype['take_profit_price'] = undefined;

/**
 * @member {module:model/TenantRestrictions} restrictions
 */
ValidateSubmitOrderRequest.prototype['restrictions'] = undefined;

/**
 * Initial capital value in USD only used to validate sells with leverage
 * @member {String} initial_capital
 */
ValidateSubmitOrderRequest.prototype['initial_capital'] = undefined;






export default ValidateSubmitOrderRequest;

