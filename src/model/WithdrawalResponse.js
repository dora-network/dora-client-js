/**
 * DORA
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Web3WithdrawalStatus from './Web3WithdrawalStatus';

/**
 * The WithdrawalResponse model module.
 * @module model/WithdrawalResponse
 * @version 1.0.0
 */
class WithdrawalResponse {
    /**
     * Constructs a new <code>WithdrawalResponse</code>.
     * A single USDC withdrawal request.
     * @alias module:model/WithdrawalResponse
     */
    constructor() { 
        
        WithdrawalResponse.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>WithdrawalResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WithdrawalResponse} obj Optional instance to populate.
     * @return {module:model/WithdrawalResponse} The populated <code>WithdrawalResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WithdrawalResponse();

            if (data.hasOwnProperty('withdrawal_id')) {
                obj['withdrawal_id'] = ApiClient.convertToType(data['withdrawal_id'], 'String');
            }
            if (data.hasOwnProperty('network_chain_id')) {
                obj['network_chain_id'] = ApiClient.convertToType(data['network_chain_id'], 'Number');
            }
            if (data.hasOwnProperty('network_name')) {
                obj['network_name'] = ApiClient.convertToType(data['network_name'], 'String');
            }
            if (data.hasOwnProperty('chain_id')) {
                obj['chain_id'] = ApiClient.convertToType(data['chain_id'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('account_id')) {
                obj['account_id'] = ApiClient.convertToType(data['account_id'], 'String');
            }
            if (data.hasOwnProperty('to_address')) {
                obj['to_address'] = ApiClient.convertToType(data['to_address'], 'String');
            }
            if (data.hasOwnProperty('quantity')) {
                obj['quantity'] = ApiClient.convertToType(data['quantity'], 'String');
            }
            if (data.hasOwnProperty('fee')) {
                obj['fee'] = ApiClient.convertToType(data['fee'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = Web3WithdrawalStatus.constructFromObject(data['status']);
            }
            if (data.hasOwnProperty('tx_hash')) {
                obj['tx_hash'] = ApiClient.convertToType(data['tx_hash'], 'String');
            }
            if (data.hasOwnProperty('failure_reason')) {
                obj['failure_reason'] = ApiClient.convertToType(data['failure_reason'], 'String');
            }
            if (data.hasOwnProperty('approved_by')) {
                obj['approved_by'] = ApiClient.convertToType(data['approved_by'], 'String');
            }
            if (data.hasOwnProperty('approved_at')) {
                obj['approved_at'] = ApiClient.convertToType(data['approved_at'], 'Date');
            }
            if (data.hasOwnProperty('settlement_transaction_id')) {
                obj['settlement_transaction_id'] = ApiClient.convertToType(data['settlement_transaction_id'], 'String');
            }
            if (data.hasOwnProperty('created_at')) {
                obj['created_at'] = ApiClient.convertToType(data['created_at'], 'Date');
            }
            if (data.hasOwnProperty('updated_at')) {
                obj['updated_at'] = ApiClient.convertToType(data['updated_at'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WithdrawalResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WithdrawalResponse</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['withdrawal_id'] && !(typeof data['withdrawal_id'] === 'string' || data['withdrawal_id'] instanceof String)) {
            throw new Error("Expected the field `withdrawal_id` to be a primitive type in the JSON string but got " + data['withdrawal_id']);
        }
        // ensure the json data is a string
        if (data['network_name'] && !(typeof data['network_name'] === 'string' || data['network_name'] instanceof String)) {
            throw new Error("Expected the field `network_name` to be a primitive type in the JSON string but got " + data['network_name']);
        }
        // ensure the json data is a string
        if (data['chain_id'] && !(typeof data['chain_id'] === 'string' || data['chain_id'] instanceof String)) {
            throw new Error("Expected the field `chain_id` to be a primitive type in the JSON string but got " + data['chain_id']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['account_id'] && !(typeof data['account_id'] === 'string' || data['account_id'] instanceof String)) {
            throw new Error("Expected the field `account_id` to be a primitive type in the JSON string but got " + data['account_id']);
        }
        // ensure the json data is a string
        if (data['to_address'] && !(typeof data['to_address'] === 'string' || data['to_address'] instanceof String)) {
            throw new Error("Expected the field `to_address` to be a primitive type in the JSON string but got " + data['to_address']);
        }
        // ensure the json data is a string
        if (data['quantity'] && !(typeof data['quantity'] === 'string' || data['quantity'] instanceof String)) {
            throw new Error("Expected the field `quantity` to be a primitive type in the JSON string but got " + data['quantity']);
        }
        // ensure the json data is a string
        if (data['fee'] && !(typeof data['fee'] === 'string' || data['fee'] instanceof String)) {
            throw new Error("Expected the field `fee` to be a primitive type in the JSON string but got " + data['fee']);
        }
        // ensure the json data is a string
        if (data['tx_hash'] && !(typeof data['tx_hash'] === 'string' || data['tx_hash'] instanceof String)) {
            throw new Error("Expected the field `tx_hash` to be a primitive type in the JSON string but got " + data['tx_hash']);
        }
        // ensure the json data is a string
        if (data['failure_reason'] && !(typeof data['failure_reason'] === 'string' || data['failure_reason'] instanceof String)) {
            throw new Error("Expected the field `failure_reason` to be a primitive type in the JSON string but got " + data['failure_reason']);
        }
        // ensure the json data is a string
        if (data['approved_by'] && !(typeof data['approved_by'] === 'string' || data['approved_by'] instanceof String)) {
            throw new Error("Expected the field `approved_by` to be a primitive type in the JSON string but got " + data['approved_by']);
        }
        // ensure the json data is a string
        if (data['settlement_transaction_id'] && !(typeof data['settlement_transaction_id'] === 'string' || data['settlement_transaction_id'] instanceof String)) {
            throw new Error("Expected the field `settlement_transaction_id` to be a primitive type in the JSON string but got " + data['settlement_transaction_id']);
        }

        return true;
    }


}



/**
 * @member {String} withdrawal_id
 */
WithdrawalResponse.prototype['withdrawal_id'] = undefined;

/**
 * Internal numeric identifier of the chain.
 * @member {Number} network_chain_id
 */
WithdrawalResponse.prototype['network_chain_id'] = undefined;

/**
 * Human-readable network name.
 * @member {String} network_name
 */
WithdrawalResponse.prototype['network_name'] = undefined;

/**
 * EVM chain ID.
 * @member {String} chain_id
 */
WithdrawalResponse.prototype['chain_id'] = undefined;

/**
 * @member {String} user_id
 */
WithdrawalResponse.prototype['user_id'] = undefined;

/**
 * @member {String} account_id
 */
WithdrawalResponse.prototype['account_id'] = undefined;

/**
 * Destination wallet address as a 0x-prefixed hex string.
 * @member {String} to_address
 */
WithdrawalResponse.prototype['to_address'] = undefined;

/**
 * Human-decimal USDC quantity to withdraw (base units divided by 10^6).
 * @member {String} quantity
 */
WithdrawalResponse.prototype['quantity'] = undefined;

/**
 * Human-decimal USDC network fee (base units divided by 10^6). 0 until the requester locks a quoted fee as part of approval.
 * @member {String} fee
 */
WithdrawalResponse.prototype['fee'] = undefined;

/**
 * @member {module:model/Web3WithdrawalStatus} status
 */
WithdrawalResponse.prototype['status'] = undefined;

/**
 * Broadcast withdraw() transaction hash as a 0x-prefixed hex string. Present from `BROADCAST` onward.
 * @member {String} tx_hash
 */
WithdrawalResponse.prototype['tx_hash'] = undefined;

/**
 * Reason the withdrawal was rejected or failed. Present for REJECTED/FAILED.
 * @member {String} failure_reason
 */
WithdrawalResponse.prototype['failure_reason'] = undefined;

/**
 * Admin who approved the withdrawal. Present once approved.
 * @member {String} approved_by
 */
WithdrawalResponse.prototype['approved_by'] = undefined;

/**
 * When the withdrawal was approved. Present once approved.
 * @member {Date} approved_at
 */
WithdrawalResponse.prototype['approved_at'] = undefined;

/**
 * Ledger settlement transaction. Present once confirmed.
 * @member {String} settlement_transaction_id
 */
WithdrawalResponse.prototype['settlement_transaction_id'] = undefined;

/**
 * @member {Date} created_at
 */
WithdrawalResponse.prototype['created_at'] = undefined;

/**
 * @member {Date} updated_at
 */
WithdrawalResponse.prototype['updated_at'] = undefined;






export default WithdrawalResponse;

